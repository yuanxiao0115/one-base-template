# 2026-03-25 验证记录

## adminManagement 终轮规则沉淀

- 结论：多轮走查中确认可复用的注意项已写入 `apps/admin/AGENTS.md`，并同步到 docs 可读版说明。
- 覆盖点：
  - 多链路请求守卫
  - 模板内联箭头函数禁令扩展
  - 统一删除链路
  - `crud.open*` 模板直调禁令
  - 嵌套表单禁止过度防御性映射
- 验证命令：
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：全部通过。

## adminManagement 末轮代码收口

- 结论：
  - 本轮要求的并行整改项已落地：`user/form.ts`、`OrgManagerDialog.vue`、菜单删除链路/操作列例外。
  - 额外横向收口已完成：`position / role / org / user` 模板直调 `crud.open*` 清零，`MenuIconInput.vue` 模板闭包清零，`tenant-info` 类型与表单映射按明确契约收口，`menu/columns.ts` 缺失值误渲染问题修复。
- 代码扫描：
  - `adminManagement` 模板内联箭头函数：未检出。
  - `adminManagement` 模板直接 `crud.openCreate/openEdit/openDetail`：未检出。
  - `adminManagement` 的 `ObActionButtons + el-dropdown`：未检出。
- 验证命令：
  - `pnpm -C apps/admin exec vp test run --config vitest.config.ts src/modules/adminManagement/user/form.unit.test.ts src/modules/adminManagement/user/components/UserBindAccountForm.unit.test.ts src/modules/adminManagement/org/components/OrgManagerDialog.unit.test.ts`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/admin build`
  - `pnpm check:admin:bundle`
  - `pnpm -C apps/admin lint:arch`
- 验证结果：
  - 测试、`typecheck`、`lint`、`build`、`check:admin:bundle`：通过。
  - `lint:arch`：未通过，但 4 处失败均为写集外历史问题 `apps/admin/src/bootstrap/material-image-service-worker.ts` 对 `import.meta.env` 的直接读取。
- 残余风险：
  - `OrgManagerDialog.vue` 仍有 `max-lines` warning，当前是可维护性低风险，不影响构建与功能。

## bootstrap lint:arch 历史项收口

- 结论：
  - `[material-image-service-worker.ts](/Users/haoqiuzhi/code/one-base-template/apps/admin/src/bootstrap/material-image-service-worker.ts)` 已不再直接读取 `import.meta.env`。
  - `admin` 的 `lint:arch` 当前恢复通过。
- 验证命令：
  - `pnpm -C apps/admin lint:arch`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin build`
  - `pnpm check:admin:bundle`
- 验证结果：全部通过。

## admin 未登录放行首页修复

- 根因：
  - `packages/core/src/stores/auth.ts` 在 `token` 模式下过度信任 `ob_auth_user` 缓存，只要本地存在用户缓存就直接判定已登录，没有校验 `tokenKey` 对应的本地 token。
- 修复：
  - `packages/core/src/createCore.ts` 新增 `auth.mode/tokenKey` 配置。
  - `packages/core/src/stores/auth.ts` 在初始化和 `ensureAuthed()` 中统一校验 token；缺 token 时清空缓存用户并返回未登录。
  - `apps/admin/src/bootstrap/{core.ts,index.ts}`、`apps/portal/src/bootstrap/index.ts`、`apps/template/src/bootstrap/index.ts` 统一向 core 传入 `authMode/tokenKey`。
  - 新增 `packages/core/src/stores/auth.test.ts` 覆盖“无 token + 残留缓存用户”回归场景。
- 同轮一并修正的历史门禁：
  - `packages/core/src/hooks/user-management-unique.test.ts` 模块路径失效。
  - `apps/admin/tests/router/assemble-routes.unit.test.ts` 固定路由断言口径落后于当前路由装配实现。
  - `packages/utils/src/storage/__tests__/local.test.ts` 假设 `window.localStorage.clear()` 在当前测试环境可直接调用。
  - `apps/docs/docs/.vitepress/config.ts` 的 readonly 数组类型与 VitePress 配置类型不兼容。
  - `apps/admin/src/services/auth/auth-avatar-preference-service.ts` 函数名不符合命名白名单。
- 验证命令：
  - `pnpm -C packages/core test:run -- src/stores/auth.test.ts`
  - `pnpm -C apps/admin test:run -- tests/router/assemble-routes.unit.test.ts`
  - `pnpm -C apps/docs typecheck`
  - `pnpm -C apps/docs build`
  - `pnpm verify`
- 验证结果：
  - 上述命令全部通过。
  - `pnpm verify` 已覆盖：命名白名单、`lint:arch`、全仓 `test:run/typecheck/lint/build`、`check:admin:bundle`。
  - 非阻断项：`apps/admin/src/modules/adminManagement/org/components/OrgManagerDialog.vue` 仍有 1 条 `max-lines` warning。

## admin 首屏守卫注册时序修复

- 浏览器复现结论：
  - Safari 中 `http://localhost:5173/` 在清掉 `localStorage.token` 后仍进入 `/home/index`。
  - 复现当时 `localStorage` 仅有 `one-base-template-admin:ob_system_current` / `one-base-template-admin:ob_theme`，`sessionStorage` 与 `document.cookie` 均为空，说明问题不在 token/cookie 残留，而在首屏守卫时序。
- 根因：
  - `apps/admin/src/bootstrap/index.ts` 先执行 `app.use(router)`，后执行 `setupRouterGuards()`。
  - Vue Router 首次导航在安装 router 时即开始，导致 `/ -> /home/index` 这次初始跳转没有经过未登录守卫。
- 修复：
  - 延后 `app.use(router)` 到 `setupRouterGuards()` 与 `installRouteDynamicImportRecovery()` 之后。
  - 新增 `apps/admin/tests/bootstrap/index.unit.test.ts` 锁定顺序：守卫注册必须早于 router 安装。
- 验证命令：
  - `pnpm -C apps/admin test:run -- tests/bootstrap/index.unit.test.ts`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/admin build`
  - Safari 浏览器复测：`localStorage.removeItem('token'); sessionStorage.clear(); location.href='/'`
- 验证结果：
  - 单测、`typecheck`、`build` 通过。
  - `lint` 通过，保留 1 条 `OrgManagerDialog.vue` 的 `max-lines` warning。
  - 浏览器复测已跳转到 `/login?redirect=/home/index`，未再直接进入系统。

## admin 已登录访问 /login 拦截修复

- 根因：
  - `packages/core/src/router/guards.ts` 在鉴权前即放行 `meta.public` 与公共路径，`/login` 未区分“已登录态”。
- 修复：
  - 守卫增加 `/login` 专项分支：已登录时自动重定向，不再进入登录页。
  - 新增安全回跳逻辑：仅允许站内路径（`/xxx` 且非 `//xxx`），非法值回落 `/`。
  - 回归测试新增：
    - 已登录访问 `/login?redirect=/system/user` -> 跳转 `/system/user`
    - 已登录访问 `/login?redirect=//evil.example` -> 跳转 `/`
    - 未登录访问 `/login` -> 允许进入登录页
- 验证命令：
  - `pnpm -C packages/core test:run -- src/router/guards.test.ts`
  - `pnpm -C apps/admin test:run -- tests/bootstrap/index.unit.test.ts tests/router/assemble-routes.unit.test.ts`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 全部通过。
  - 测试过程中出现 `--localstorage-file` warning，为测试环境提示，不影响结论。

## PortalManagement 删除 designer 历史 alias

- 根因：
  - `PortalDesigner` 仍保留 `route.alias`（`/resource/portal/setting`），会引入额外可访问入口，不利于路由收口与审计。
- 修复：
  - 删除 `apps/admin/src/modules/PortalManagement/routes/standalone.ts` 中 `PortalDesigner` 的 `alias` 配置。
  - 更新路由装配回归测试，新增 `PortalDesigner.alias === undefined` 断言，防止 alias 回流。
  - 同步更新 `apps/admin/AGENTS.md` 与 `apps/docs/docs/guide/portal/admin-designer.md` 的规则口径。
- 验证命令：
  - `pnpm -C apps/admin test:run -- tests/router/assemble-routes.unit.test.ts`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 全部通过。
  - `apps/admin` 测试进程中的 `--localstorage-file` warning 为环境提示，不影响结论。

## 路由策略清单 + meta 收敛 + 最小鉴权链路

- 结论：
  - 路由策略治理已形成“自动产物 + 校验门禁”：`buildRoutePolicyReport` 会输出 `public/skipMenuAuth/activePath` 清单并落盘 `.codex/route-policy/admin-route-policy.json`。
  - 模块路由 meta 已统一收敛到 `@/router/meta` helper，避免继续散写 `meta: {}` 造成漏配。
  - 最小鉴权链路已形成快速回归命令：未登录受保护页、已登录访问 `/login`、SSO 回调落地三条链路可一键验证。
- 关键改动：
  - 新增：`apps/admin/src/router/{meta.ts,route-policy.ts}`
  - 新增：`apps/admin/tests/router/route-policy.unit.test.ts`
  - 新增：`apps/admin/tests/architecture/route-meta-helper-source.unit.test.ts`
  - 新增：`packages/core/src/router/auth-minimal-e2e.unit.test.ts`
  - 新增：`apps/admin/tests/services/auth/sso-minimal-e2e.unit.test.ts`
  - 更新：7 个模块路由文件统一使用 meta helper。
  - 更新：`apps/admin/package.json`、根 `package.json` 新增最小链路脚本。
  - 更新：`apps/docs/docs/guide/menu-route-spec.md`（补充 meta helper 与门禁命令）。
- 验证命令：
  - `pnpm -C packages/core test:run -- src/router/auth-minimal-e2e.unit.test.ts`
  - `pnpm -C apps/admin check:route-policy`
  - `pnpm -C apps/admin test:e2e:minimal-auth`
  - `pnpm test:e2e:minimal-auth`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 以上命令均通过。
  - 非阻断历史项：`OrgManagerDialog.vue` 的 `max-lines` warning 仍存在，不影响本轮路由能力落地。

## 登录路由板块专项收口（稳定性/扩展性/性能）

- 结论：
  - 已完成登录路由三类改进：
    - 稳定性：cookie 模式首次守卫强校验会话，避免缓存误判已登录。
    - 可扩展性：守卫提供 `resolveAuthedLoginRedirect` 扩展点，应用层可注入子路径部署回跳解析。
    - 性能：登录页 direct token 场景优先执行跳转，不再被登录配置接口阻塞。
  - SSO 异常路径补齐 `idToken` 清理，降低残留状态风险。
- 关键改动：
  - `packages/core/src/stores/auth.ts`
  - `packages/core/src/createCore.ts`
  - `packages/core/src/router/guards.ts`
  - `apps/admin/src/bootstrap/index.ts`
  - `apps/admin/src/pages/login/LoginPage.vue`
  - `apps/admin/src/pages/sso/SsoCallbackPage.vue`
  - 测试：`packages/core/src/{stores/auth.test.ts,router/guards.test.ts}`、`apps/admin/tests/{bootstrap/index.unit.test.ts,architecture/login-route-robustness-source.unit.test.ts}`
- 验证命令：
  - `pnpm -C packages/core test:run -- src/stores/auth.test.ts src/router/guards.test.ts`
  - `pnpm -C apps/admin test:run -- tests/bootstrap/index.unit.test.ts`
  - `pnpm test:e2e:minimal-auth`
  - `pnpm -C packages/core typecheck`
  - `pnpm -C packages/core lint`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 全部通过。
  - 保留历史非阻断项：`OrgManagerDialog.vue` 的 `max-lines` warning。

## 登录后访问 /login 仍可进入页（token 缺失边界）

- 结论：
  - 已修复 `token` 模式下“本地 token 缺失但服务端会话仍有效”导致 `/login` 被误放行的问题。
  - 修复后策略：首次守卫仅在“缺 token + 有缓存用户”时探测一次 `fetchMe()`；成功视为已登录并阻止进入登录页，失败清缓存并保持未登录。
- 关键改动：
  - `packages/core/src/stores/auth.ts`
  - `packages/core/src/stores/auth.test.ts`
  - `packages/core/README.md`
  - `apps/docs/docs/guide/module-system.md`
- 验证命令：
  - `pnpm -C packages/core test:run -- src/stores/auth.test.ts`
  - `pnpm -C packages/core test:run -- src/stores/auth.test.ts src/router/guards.test.ts src/router/auth-minimal-e2e.unit.test.ts`
  - `pnpm -C apps/admin test:run -- tests/bootstrap/index.unit.test.ts`
  - `pnpm test:e2e:minimal-auth`
  - `pnpm -C packages/core typecheck`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。
  - 历史非阻断项仍存在：`apps/admin/src/modules/adminManagement/org/components/OrgManagerDialog.vue` 的 `max-lines` warning。

## 登录后访问 /login 仍可进入页（二次收口）

- 结论：
  - 已进一步增强为“首次缺 token 一律探测会话”，不再依赖本地缓存用户存在。
  - 该策略可覆盖“无 `ob_auth_user` 缓存但服务端仍已登录”的边界，降低误进登录页概率。
- 关键改动：
  - `packages/core/src/stores/auth.ts`
  - `packages/core/src/stores/auth.test.ts`
  - `packages/core/README.md`
  - `apps/docs/docs/guide/module-system.md`
- 验证命令：
  - `pnpm -C packages/core test:run -- src/stores/auth.test.ts src/router/guards.test.ts src/router/auth-minimal-e2e.unit.test.ts`
  - `pnpm test:e2e:minimal-auth`
  - `pnpm -C packages/core typecheck`
  - `pnpm -C packages/core lint`
  - `pnpm -C apps/admin test:run -- tests/bootstrap/index.unit.test.ts`
- 验证结果：
  - 上述命令全部通过。

## 登录页入口兜底：已登录访问 /login 立即回跳

- 结论：
  - 除全局路由守卫外，登录页入口新增二次兜底判断：已登录态进入 `/login` 将立即回跳目标页。
  - 该兜底位于登录页 `onMounted`，执行时机在“直登 token 分支之后、登录配置加载之前”。
- 关键改动：
  - `apps/admin/src/pages/login/LoginPage.vue`
  - `apps/admin/tests/architecture/login-route-robustness-source.unit.test.ts`
- 验证命令：
  - `pnpm -C apps/admin test:run -- tests/architecture/login-route-robustness-source.unit.test.ts tests/bootstrap/index.unit.test.ts`
  - `pnpm test:e2e:minimal-auth`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
- 验证结果：
  - 上述命令全部通过。
  - 历史非阻断项：`apps/admin/src/modules/adminManagement/org/components/OrgManagerDialog.vue` 的 `max-lines` warning 仍存在。

## 全局守卫收口：/login 按 token 有无分支（用户指定口径）

- 结论：
  - 登录态判断收口到全局守卫，登录页不再承载二次回跳判断。
  - `/login` 在 `token/mixed` 模式按 token 有无分支：无 token 直接进入登录页，有 token 再走 `ensureAuthed`。
- 关键改动：
  - `packages/core/src/router/guards.ts`
  - `packages/core/src/router/guards.test.ts`
  - `apps/admin/src/pages/login/LoginPage.vue`
  - `apps/admin/tests/architecture/login-route-robustness-source.unit.test.ts`
  - `packages/core/README.md`
  - `apps/docs/docs/guide/module-system.md`
- 验证命令：
  - `pnpm -C packages/core test:run -- src/router/guards.test.ts src/router/auth-minimal-e2e.unit.test.ts`
  - `pnpm -C apps/admin test:run -- tests/architecture/login-route-robustness-source.unit.test.ts tests/bootstrap/index.unit.test.ts`
  - `pnpm test:e2e:minimal-auth`
  - `pnpm -C packages/core typecheck`
  - `pnpm -C packages/core lint`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。

## 路由 access 模型重构（open/auth/menu）

- 结论：
  - admin/core 路由访问模型已统一为 `meta.access = open | auth | menu`，未声明时默认按 `menu` 处理。
  - 登录页、SSO、403/404、首页、门户独立页、401 未授权回跳链路都已按新模型收口。
  - 旧 `skipMenuAuth` 口径已从实现、测试、AGENTS、文档中清理。
- 关键改动：
  - `packages/core/src/router/guards.ts`
  - `packages/core/src/router/route-access.ts`
  - `packages/core/src/router/fixed-routes.ts`
  - `packages/core/src/menu/fromRoutes.ts`
  - `apps/admin/src/router/{meta.ts,public-routes.ts,assemble-routes.ts,route-policy.ts}`
  - `apps/admin/src/bootstrap/{index.ts,http.ts}`
  - `apps/admin/AGENTS.md`
  - `apps/docs/docs/guide/{menu-route-spec.md,module-system.md,layout-menu.md,architecture-runtime-deep-dive.md,architecture.md}`
- 验证：
  - `pnpm -C packages/core test:run -- src/router/guards.test.ts src/router/fixed-routes.test.ts src/router/route-diagnostics.test.ts src/router/module-assembly.test.ts src/router/route-signature.test.ts src/menu/fromRoutes.test.ts`
  - `pnpm -C apps/admin test:run -- tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/bootstrap/index.unit.test.ts tests/bootstrap/http.unit.test.ts tests/services/auth/auth-scenario-provider.unit.test.ts tests/services/auth/sso-minimal-e2e.unit.test.ts`
  - `pnpm -w typecheck`
  - `pnpm -C packages/core build`
  - `pnpm -C apps/admin build`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/docs build`
- 结果：以上命令全部通过。

## /sso 认证入口语义统一

- 结论：
  - 本轮未改动路由守卫行为，只统一 `/sso` 在 README、AGENTS、注释、文档中的语义描述。
  - 当前口径固定为：`/sso` 是“认证入口 / SSO 回调入口”，未登录允许进入完成回调，已登录再次访问时由全局守卫拦截并安全回跳。
- 关键改动：
  - `README.md`
  - `apps/admin/AGENTS.md`
  - `apps/admin/src/config/sso.ts`
  - `apps/admin/src/router/public-routes.ts`
  - `apps/admin/src/router/meta.ts`
  - `packages/core/src/router/guards.ts`
  - `apps/docs/docs/guide/module-system.md`
- 验证命令：
  - `pnpm -C packages/core test:run -- src/router/guards.test.ts src/router/auth-minimal-e2e.unit.test.ts`
  - `pnpm -C apps/admin test:run -- tests/services/auth/sso-minimal-e2e.unit.test.ts tests/bootstrap/index.unit.test.ts`
  - `pnpm -C apps/admin build`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 以上命令全部通过。
  - 测试期间出现的 `--localstorage-file` warning 为环境提示，不影响回归结论。
