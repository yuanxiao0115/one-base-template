# 2026-03-28 验证记录

## document-form-engine 设计器画布切换 Univer

- 结论：
  - `packages/document-form-engine` 设计态画布已切换到 Univer。
  - 保持“自定义物料层 + Vue 运行态”主线不变：物料定义与 `runtime/renderer.ts` 契约未破坏。
  - 节点选中联动与拖拽排版（SelectionMoveStart/SelectionMoveEnd 回写 anchor）已落地。
- 关键产物：
  - `packages/document-form-engine/designer/UniverDocumentCanvas.vue`
  - `packages/document-form-engine/designer/canvas-bridge.ts`
  - `packages/document-form-engine/designer/DocumentDesignerWorkbench.vue`
  - `packages/document-form-engine/designer/useDocumentDesignerState.ts`
  - `packages/document-form-engine/tests/designer-canvas-bridge.test.ts`
  - `packages/document-form-engine/tests/designer-state.test.ts`
  - `apps/docs/docs/guide/document-form-designer.md`
- 验证命令：
  - `pnpm -C packages/document-form-engine test:run`
  - `pnpm -C packages/document-form-engine typecheck`
  - `pnpm -C packages/document-form-engine build`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。
  - `pnpm -C packages/document-form-engine lint` 仍受现有 `vite-plus` 配置解析异常影响（`callablePlugin.getOrder is not a function`），未阻断本次功能链路验证。

## document-form-engine SheetSchema v2（完整产品化 Phase 2）

- 结论：
  - 模板协议已升级到 `version: '2'`，并新增 `sheet` 配置（`merges/styles/rowHeights/columnWidths/viewport`）。
  - `parseDocumentTemplate()` 已支持 `v1 -> v2` 自动迁移，历史模板可直接加载并补齐默认 `sheet`。
  - 文档已补齐“合并单元格、边框线色、行高列宽”的配置入口说明。
- 关键产物：
  - `packages/document-form-engine/schema/sheet.ts`
  - `packages/document-form-engine/schema/types.ts`
  - `packages/document-form-engine/schema/template.ts`
  - `packages/document-form-engine/index.ts`
  - `packages/document-form-engine/tests/sheet-schema.test.ts`
  - `apps/docs/docs/guide/document-form-designer.md`
  - `docs/plans/2026-03-28-document-form-engine-full-product-plan.md`
- 验证命令：
  - `pnpm -C packages/document-form-engine test:run -- tests/sheet-schema.test.ts`
  - `pnpm -C packages/document-form-engine test:run`
  - `pnpm -C packages/document-form-engine typecheck`
  - `pnpm -C packages/document-form-engine build`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。

## document-form-engine 物料样式协议与右侧配置面（完整产品化 Phase 3/4）

- 结论：
  - 物料协议已升级：默认物料全部补齐 `sheetLayout` 与 `stylePreset`。
  - 设计器右侧已支持：
    - 合并区域管理（按当前物料区域添加/删除）
    - 单元格样式应用（边框线色、填充、字体、对齐、换行）
  - 样式与合并配置统一落到 `template.sheet.styles` / `template.sheet.merges`。
- 关键产物：
  - `packages/document-form-engine/materials/sheet-style.ts`
  - `packages/document-form-engine/materials/types.ts`
  - `packages/document-form-engine/materials/default-materials.ts`
  - `packages/document-form-engine/designer/sheet-ops.ts`
  - `packages/document-form-engine/designer/panels/SheetStyleEditor.vue`
  - `packages/document-form-engine/designer/panels/MergeEditor.vue`
  - `packages/document-form-engine/designer/DocumentPropertyInspector.vue`
  - `packages/document-form-engine/designer/DocumentDesignerWorkbench.vue`
  - `packages/document-form-engine/tests/material-sheet-style.test.ts`
  - `packages/document-form-engine/tests/designer-sheet-ops.test.ts`
- 验证命令：
  - `pnpm -C packages/document-form-engine test:run`
  - `pnpm -C packages/document-form-engine typecheck`
  - `pnpm -C packages/document-form-engine build`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。

## document-form-engine 运行态/打印态 sheet 对齐（完整产品化 Phase 5）

- 结论：
  - 运行态已可构建 `sheet` 渲染模型并消费模板配置。
  - 打印态已复用同一套 `sheet` 渲染模型，运行态/打印态输出同构。
  - 样式解析采用统一优先级：模板区域样式覆盖物料默认预设。
- 关键产物：
  - `packages/document-form-engine/runtime/sheet-renderer.ts`
  - `packages/document-form-engine/runtime/print-renderer.ts`
  - `packages/document-form-engine/tests/runtime-sheet-parity.test.ts`
  - `packages/document-form-engine/index.ts`
- 验证命令：
  - `pnpm -C packages/document-form-engine test:run`
  - `pnpm -C packages/document-form-engine typecheck`
  - `pnpm -C packages/document-form-engine build`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。

## admin 模板生命周期接入（完整产品化 Phase 6）

- 结论：
  - admin 侧已接入模板生命周期服务（草稿/发布/回滚）。
  - 设计页可直接执行发布与回滚，预览页优先展示发布模板。
  - 引擎注册层已支持 `templateService` 注入与覆盖，便于后续替换真实接口实现。
- 关键产物：
  - `apps/admin/src/modules/DocumentFormManagement/services/template-service.ts`
  - `apps/admin/src/modules/DocumentFormManagement/services/template-service.unit.test.ts`
  - `apps/admin/src/modules/DocumentFormManagement/engine/register.ts`
  - `apps/admin/src/modules/DocumentFormManagement/engine/register.unit.test.ts`
  - `apps/admin/src/modules/DocumentFormManagement/designPage/DocumentFormDesignerPage.vue`
  - `apps/admin/src/modules/DocumentFormManagement/designPage/DocumentFormPreviewPage.vue`
- 验证命令：
  - `pnpm -C apps/admin test:run:file -- src/modules/DocumentFormManagement/services/template-service.unit.test.ts src/modules/DocumentFormManagement/engine/register.unit.test.ts`
  - `pnpm -C apps/admin typecheck`
  - `pnpm -C apps/admin lint`
  - `pnpm -C apps/docs lint`
  - `pnpm -C apps/docs build`
- 验证结果：
  - 上述命令全部通过。
