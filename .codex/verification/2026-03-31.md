# 2026-03-31 验证记录

## ObTable 命名与样式收口

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/types.ts`
- `packages/ui/src/styles/table-theme.css`
- `packages/ui/src/table-source.test.ts`
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/table-vxe-migration.md`
- `docs/plans/2026-03-31-element-table-wrapper-design.md`
- `docs/plans/2026-03-31-element-table-wrapper-plan.md`

### 结论

- `ObTable` 壳组件已完成本轮收口：外层组件名为 `Table`，包装类统一为 `ob-table`，分页 total 左侧偏移与垂直居中已生效。
- 表格共享视觉基线已更新为：字号 `14px`、表头字重 `600`、内容字重 `500`、表头背景 `#f8f8f8`、表头与内容文字颜色 `#333`。
- 滚动条样式已统一到共享 token：颜色 `#DCDEE0`、可视宽度 `8px`、圆角 `6px`；`ObTable` 与消费共享 token 的 `ObVxeTable` 保持一致。
- `ObTable.pagination` 已直接复用 `@one-base-template/core` 的 `PaginationConfig` 类型，`apps/admin` 页面不再出现分页配置类型不兼容。
- 文档与规则口径已同步更新为 `ObTable`，不再对外暴露 `ObElementTable` 命名。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：源码测试 `9/9` 通过；`typecheck`、`lint`、`build` 通过。
- `apps/admin`：5 个页面源码测试通过；`typecheck` 与 `build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 风险备注

- 当前仅收口自定义壳组件 `ObTable` 与共享 token；`Table.vue` 内部 `ElementTable*` 类型命名仍保留，属于实现细节，不影响对外 API 与页面使用。

## tokenKey 污染修复（多子项目同域隔离）

### 范围

- `packages/core/src/config/platform-config.ts`
- `packages/core/src/config/platform-config.test.ts`
- `apps/admin/src/config/platform-config.ts`
- `apps/admin-lite/src/config/platform-config.ts`
- `apps/zfw-system-sfss/src/config/platform-config.ts`
- `apps/portal/public/platform-config.json`
- `apps/admin/tests/config/platform-config.unit.test.ts`
- `apps/admin-lite/tests/config/platform-config.unit.test.ts`
- `apps/zfw-system-sfss/tests/config/platform-config.unit.test.ts`
- `apps/docs/docs/guide/env.md`
- `README.md`

### 结论

- preset 模式下，`tokenKey/idTokenKey` 支持按 `storageNamespace`（未配置时回退 `appcode`）自动生成，默认形态为 `${scope}-token` 与 `${scope}-id-token`。
- `apps/admin`、`apps/admin-lite`、`apps/zfw-system-sfss` 已移除硬编码 `token/idToken`，统一走自动生成，避免多个子项目本地并行启动时 token 污染。
- `apps/portal` 运行时配置补充了 `storageNamespace=one-base-template-portal`，使 portal 的 token key 与其他子项目隔离。
- 三个应用的 `platform-config` 单测新增 key 断言，防止回归为通用 `token`。

### 验证命令

```bash
pnpm -C packages/core test:run src/config/platform-config.test.ts
pnpm -C packages/core typecheck
pnpm -C apps/admin test:run:file -- tests/config/platform-config.unit.test.ts
pnpm -C apps/admin-lite test:run:file -- tests/config/platform-config.unit.test.ts
pnpm -C apps/zfw-system-sfss test:run tests/config/platform-config.unit.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin-lite typecheck
pnpm -C apps/zfw-system-sfss typecheck
pnpm -C apps/portal typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/core`：配置解析测试与 `typecheck` 通过。
- `apps/admin` / `apps/admin-lite` / `apps/zfw-system-sfss`：定向单测与 `typecheck` 通过。
- `apps/portal`：`typecheck` 通过。
- `apps/docs`：`lint` 0 warning/0 error，`build` 成功。

### 风险备注

- 本次改动会导致历史本地 `localStorage['token']` 不再被读取；首次切换到新版本时需要重新登录一次。

---

## admin-lite README 快速使用手册补充

### 范围

- `apps/admin-lite/README.md`
- `apps/admin-lite/.env.example`

### 结论

- `admin-lite` 新增“快速使用手册”，明确 `apiBaseUrl/baseUrl` 配置入口与差异。
- README 增补“配置入口速查”表，覆盖网关地址、路由基路径、平台配置、token key、UI、布局与 SSO。
- `.env.example` 中平台配置路径说明已修正为 `apps/admin-lite/src/config/platform-config.ts`，避免误导到 admin 主应用。

### 验证命令

```bash
pnpm -C apps/admin-lite lint
```

### 验证结果

- `apps/admin-lite`：`lint` 通过（0 warning / 0 error）。

---

## historyMode + 统一前缀配置（防静态资源路径错位）

### 范围

- `packages/core/src/config/platform-config.ts`
- `packages/core/src/config/platform-config.test.ts`
- `packages/core/src/index.ts`
- `scripts/vite/app-base.ts`
- `apps/admin/src/config/env.ts`
- `apps/admin/src/config/platform-config.ts`
- `apps/admin/src/bootstrap/index.ts`
- `apps/admin/vite.config.ts`
- `apps/admin/tests/config/platform-config.unit.test.ts`
- `apps/admin/.env.example`
- `apps/admin-lite/src/config/env.ts`
- `apps/admin-lite/src/config/platform-config.ts`
- `apps/admin-lite/src/bootstrap/index.ts`
- `apps/admin-lite/vite.config.ts`
- `apps/admin-lite/tests/config/platform-config.unit.test.ts`
- `apps/admin-lite/.env.example`
- `apps/admin-lite/README.md`
- `apps/portal/public/platform-config.json`
- `apps/portal/src/config/env.ts`
- `apps/portal/src/router/index.ts`
- `apps/portal/vite.config.ts`
- `README.md`
- `apps/docs/docs/guide/env.md`
- `apps/docs/docs/guide/menu-route-spec.md`
- `apps/docs/docs/guide/architecture-runtime-deep-dive.md`

### 结论

- 平台配置契约已新增 `historyMode`，并支持 `history/hash` 枚举校验；preset 默认值为 `history`。
- `admin`、`admin-lite`、`portal` 已统一按 `historyMode` 创建 router history，支持 hash 模式切换。
- 新增 `VITE_APP_BASE` 统一前缀入口，Vite `base` 与 router `baseUrl` 已同源，减少子路径部署时静态资源与路由前缀错位。
- `admin-lite` 快速手册已补齐：`baseUrl` 配置位置、`historyMode` 入口、`hash` 可选说明。
- `apps/zfw-system-sfss` 已从工作区移除，本轮结论仅覆盖现存应用。

### 验证命令

```bash
pnpm -C packages/core test:run src/config/platform-config.test.ts
pnpm -C packages/core typecheck
pnpm -C apps/admin test:run:file -- tests/config/platform-config.unit.test.ts
pnpm -C apps/admin test:run:file -- tests/config/env.unit.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/admin-lite test:run:file -- tests/config/platform-config.unit.test.ts
pnpm -C apps/admin-lite test:run:file -- tests/config/env.unit.test.ts
pnpm -C apps/admin-lite typecheck
pnpm -C apps/admin-lite build
pnpm -C apps/portal typecheck
pnpm -C apps/portal build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/core`：定向测试与 `typecheck` 通过。
- `apps/admin`：定向测试、`typecheck`、`build` 均通过。
- `apps/admin-lite`：定向测试、`typecheck`、`build` 均通过。
- `apps/portal`：`typecheck`、`build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- hash 模式下 URL 会带 `#`，如接入网关统一鉴权或埋点系统，需要确认其对 hash 路由解析策略一致。

---

## TanStack Table 并行封装（packages/ui）

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/components/table/internal/tanstack-pagination.ts`
- `packages/ui/src/components/table/types.ts`
- `packages/ui/src/styles/table-theme.css`
- `packages/ui/src/styles/vxe-theme.css`
- `packages/ui/src/components/table/VxeTable.vue`
- `packages/ui/src/index.ts`
- `packages/ui/src/plugin.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `packages/ui/src/index.test.ts`
- `packages/ui/src/plugin.test.ts`
- `packages/ui/package.json`
- `pnpm-lock.yaml`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTanStackTable` 已在 `packages/ui` 封装完成，并保持与 `ObVxeTable` 相同的核心契约（事件、分页、expose）。
- 样式层已收口到共享 `--ob-table-*` token，VXE 与 TanStack 共用同一主题基线。
- 插件注册与入口导出已完成，可通过 `ObTanStackTable`/`TanStackTable` 直接使用。
- 依赖与锁文件已同步，admin 构建与体积预算检查通过。

### 验证命令

```bash
pnpm install
pnpm -C packages/ui lint
pnpm -C packages/ui typecheck
pnpm exec vp test run packages/ui/src/tanstack-table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm check:admin:bundle
```

### 验证结果

- `packages/ui`：`lint` 0 warning / 0 error；`typecheck` 通过。
- `packages/ui`：`tanstack-table-source/index/plugin` 三个测试文件全部通过（7/7）。
- `apps/admin`：`typecheck` 与 `build` 通过。
- `check:admin:bundle`：预算检查全部 PASS。

### 风险备注

- 当前 `ObTanStackTable` 先以“契约对齐 + 样式一致”为第一目标，复杂页面（超大数据量、极端复杂嵌套表头）仍建议按模块灰度验证后再全面替换。

### 二次评审收口（阻塞项修复后）

- fixed 列：已补齐 sticky 偏移与左右边缘阴影，不再仅停留在 class 标记。
- custom 排序：`sortable: 'custom'` 已改为仅触发排序事件，不进行本地重排；并补齐 `sortBy/prop` 字段映射。
- 树表：已补齐 `trigger='cell'` 与 `reserve` 展开态保留行为。
- slot 参数：已补充 `size`，兼容现有 `operation` 插槽使用方式。
- 二次代码审查结论：无阻塞项。

### 残余风险（非阻塞）

- `sortBy` 的函数形态目前仍未消费（当前仓库未检索到实际用例）。
- 当前门禁测试以源码断言为主，后续可继续补运行态挂载回归。

---

## Log 模块灰度：登录日志页切 TanStack

### 范围

- `apps/admin/src/modules/LogManagement/login-log/list.vue`
- `apps/admin/src/modules/LogManagement/login-log/list.source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `apps/admin` 的登录日志页已从 `ObVxeTable` 切换到 `ObTanStackTable`，分页与操作列插槽交互保持不变。
- 该变更作为 Log 模块灰度首站，后续可按同模板逐页推进到 `sys-log`。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `list.source.test.ts` 通过（1/1）。
- `apps/admin`：`typecheck`、`build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 本轮仅灰度登录日志页；`sys-log` 仍使用 `ObVxeTable`，如需整模块切换需再做一轮替换与回归。

---

## 样式污染排查：body 8px margin

### 范围

- `packages/ui/src/index.ts`
- `packages/ui/src/styles/table-theme.css`
- `packages/ui/src/styles/vxe-theme.css`
- `packages/ui/src/components/table/VxeTable.vue`
- `packages/ui/src/components/table/TanStackTable.vue`
- `apps/admin/src/styles/index.css`
- `apps/admin-lite/src/styles/index.css`

### 结论

- `body 8px margin` 不是本轮表格封装新增规则写入导致，而是 admin/admin-lite 全局样式未显式重置 body 外边距，触发浏览器默认 UA 样式（8px）。
- 已在 `apps/admin` 与 `apps/admin-lite` 全局样式入口补齐 `margin: 0;`，页面外围白边问题消失。
- 额外确认的“可见全局影响点”：
  - `packages/ui/src/index.ts` 仍会全局引入 `vxe-pc-ui/lib/style.css`、`vxe-table/lib/style.css`；
  - `vxe-pc-ui` 存在 `html[data-vxe-lock-scroll] body{...}` 的锁滚动规则；
  - `packages/ui` 表格主题文件通过 `:root` 与 `[data-vxe-ui-theme]` 注入变量（变量级全局影响）。

### 验证命令

```bash
pnpm -C apps/admin lint
pnpm -C apps/admin-lite lint
```

### 验证结果

- `apps/admin`：lint 通过（0 error，保留 1 条既有 warning）。
- `apps/admin-lite`：lint 通过（0 warning / 0 error）。

### 风险备注

- 当前修复解决了 `body` 默认边距问题；若后续目标是进一步降低 UI 包“全局样式影响面”，可再把 VXE 样式引入策略改为按需加载（需要单独评估迁移成本）。

---

## 菜单折叠按钮样式受影响排查

### 范围

- `packages/ui/src/layouts/modes/SideLayout.vue`

### 结论

- 侧栏底部折叠按钮是原生 `button`，此前样式缺少基础重置（`border/background/padding/appearance`），对外部全局样式与浏览器默认样式敏感。
- 本次已在组件内补齐关键重置属性，按钮视觉与交互不再依赖外界样式基线。

### 验证命令

```bash
pnpm -C packages/ui lint
pnpm -C packages/ui typecheck
```

### 验证结果

- `packages/ui`：`lint` 0 warning / 0 error。
- `packages/ui`：`typecheck` 通过。

### 风险备注

- 本次修复仅覆盖 `side` 布局折叠按钮；若后续新增同类原生按钮，建议沿用同样的“组件内显式重置”策略。

---

## TanStack 分页切换（VxePager -> Element Pagination）

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/components/table/internal/tanstack-pagination.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTanStackTable` 已移除 `VxePager` 依赖，分页渲染改为 `el-pagination`。
- 分页事件契约保持不变，仍由组件向外抛出：`page-current-change` / `page-size-change`。
- 文档已同步更新为 Element 分页口径。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui` 源码门禁测试通过（`6/6`）。
- `packages/ui`：`typecheck` 与 `lint` 均通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 当前 `ObTanStackTable` 仍保留 `virtualConfig` props 但未消费；后续若推进大数据场景，建议补齐 TanStack 虚拟滚动接入。

---

## ObTanStackTable 滚动透底修复

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 粘性表头/fixed 列不再依赖半透明 token 直接贴在滚动内容之上，改为叠层背景后可避免“滚动露底”。
- fixed 列背景由样式类统一控制，移除 `resolveFixedStyle` 内联背景后，斑马纹与固定列样式不再互相覆盖。
- 文档已补充 TanStack 滚动稳定性说明，和当前实现保持一致。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui` 源码门禁测试通过（`6/6`）。
- `packages/ui`：`typecheck` 与 `lint` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 本次修复针对 sticky/fixed 透底问题，不涉及虚拟滚动渲染路径；若后续启用 `virtualConfig`，需再做一次滚动样式回归。

---

## ObTanStackTable 能力补齐（2/3/4/5/9）

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTanStackTable` 已补齐本轮目标能力：列显隐、列宽调整、列顺序拖拽、虚拟滚动、跨页选择。
- 跨页选择通过 `reserveSelection` 开关控制，并新增 `getSelectedRowKeys/setSelectedRowKeys` 能力，兼容迁移期编程式控制。
- 文档已同步补充 2/3/4/5/9 能力口径、接入示例与使用约束。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`10/10`）。
- `packages/ui`：`typecheck` 与 `lint` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 列顺序拖拽当前按可见表头执行，复杂多级表头场景建议先按页面灰度验证再批量推广。
- 虚拟滚动默认按固定行高估算，若页面存在动态行高，建议先关闭或按业务场景单独评估参数。

---

## admin 角色域灰度（角色管理 + 角色分配）

### 范围

- `apps/admin/src/modules/adminManagement/role/list.vue`
- `apps/admin/src/modules/adminManagement/role-assign/list.vue`
- `apps/admin/src/modules/adminManagement/role/list.source.test.ts`
- `apps/admin/src/modules/adminManagement/role-assign/list.source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `角色管理` 与 `角色分配` 列表页已切换为 `ObTanStackTable`，分页与操作列插槽契约保持不变。
- 角色域已具备与登录日志页一致的 TanStack 灰度基线，便于后续模块扩散。
- 文档已同步灰度范围，避免口径滞后。

### 验证命令

```bash
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin build
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `apps/admin`：源码门禁测试通过（`3/3`），`typecheck` 与 `build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 当前灰度范围仍在 `apps/admin`；`apps/admin-lite` 的角色域页面尚未同步切换，如需双应用一致需再做一轮替换与回归。

---

## ObTanStackTable 分页中文化

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTanStackTable` 分页器已强制使用 `zh-cn` locale，分页文案统一中文展示。
- 即使外层应用未显式配置 Element 全局中文，TanStack 分页区仍可稳定输出中文。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`10/10`），`typecheck/lint` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 该策略是组件级中文强制；若后续需要多语言切换，需要把 locale 从固定值改为上层可配置输入。

---

## 菜单管理树形表格（TanStack）

### 范围

- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/admin/src/modules/adminManagement/menu/list.vue`
- `apps/admin/src/modules/adminManagement/menu/list.source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 菜单管理页已切换到 `ObTanStackTable`，树表与操作列交互保持可用。
- TanStack 引擎已支持 `treeConfig.expandAll`，可满足菜单管理“树默认展开”需求。
- 菜单管理树表能力已形成源码门禁与文档口径，具备回归防护。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin test:run:file -- src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts src/modules/LogManagement/login-log/list.source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin build
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`10/10`），`typecheck/lint` 通过。
- `apps/admin`：源码门禁测试通过（`4/4`），`typecheck/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 目前 `expandAll` 采用“数据更新时重算展开态”策略；若后续需要“仅首次展开、后续保持用户手动折叠状态”，需再加可配置策略开关。

---

## TanStack 列宽 width/minWidth 生效修复

### 范围

- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 已修复 `width/minWidth` 被默认列宽覆盖的问题。
- 现在列宽策略为：
  - 配置 `width` 或发生手动拖拽时，列宽按当前 size 输出（含 `maxWidth` 约束）；
  - 仅配置 `minWidth` 时，不再强制注入默认 `width`，可按 `minWidth` 生效。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin test:run:file -- src/modules/adminManagement/menu/list.source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。
- `apps/admin`：菜单管理源码门禁测试通过（`1/1`）。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 当前修复以“显式宽度优先 + 最小宽度兜底”为策略；若后续需要百分比列宽语义（如 `30%`）完整支持，需要再补百分比宽度在 TanStack size 与 fixed offset 计算中的专门处理。

---

## 树表展开按钮替换为设计稿 SVG

### 范围

- `packages/ui/src/components/table/assets/tree-toggle-collapsed.svg`
- `packages/ui/src/components/table/assets/tree-toggle-expanded.svg`
- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- TanStack 树表展开按钮已从 Iconify 切换为本地 SVG，并按未展开/已展开状态切换图标。
- 样式交互保持按钮点击区与 hover/focus 态，视觉与用户提供素材一致。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin test:run:file -- src/modules/adminManagement/menu/list.source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。
- `apps/admin`：菜单管理源码门禁测试通过（`1/1`）。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 当前图标资源采用本地静态 SVG；若后续设计稿替换频繁，建议沉淀统一图标资源目录与命名版本策略，减少跨模块替换成本。

---

## 修复 tree toggle SVG 路径解析

### 范围

- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/env.d.ts`
- `packages/ui/src/tanstack-table-source.test.ts`

### 结论

- 已修复 `vite:import-analysis` 在 admin 联调场景下对 tree toggle SVG 的解析失败问题。
- 资源加载方式统一为静态 `import '*.svg'`，并补齐 ui 包内 `svg` 类型声明。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。
- `apps/admin`：`typecheck/build` 通过，未再出现 tree toggle SVG 解析异常。

### 风险备注

- 当前方案依赖 Vite 的静态资源导入语义；若后续构建链切换为非 Vite 打包器，需要确认其对 `svg` 资源导入兼容性。

---

## 树表图标间距与同级左对齐细化

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`

### 结论

- 树表图标与文字间距已统一为 `8px`。
- 无子节点行保留图标同宽占位，保证同级文本起点左对齐。
- 鼠标移动到图标时展示可点击态。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。

### 风险备注

- 图标点击区域当前与资源宽度一致（`16px`）；若后续需要更大可点热区，可在不改变视觉尺寸的前提下再加外层透明 hit-area。

---

## 树表 icon/content 间距与占位宽度精确对齐

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`

### 结论

- `ob-tanstack-table__tree-toggle-icon` 与 `ob-tanstack-table__tree-content` 间距固定为 `8px`。
- `ob-tanstack-table__tree-placeholder` 与图标宽度保持一致（同用 `--ob-table-tree-toggle-icon-size`），并同样保留 `8px` 间距。
- 图标 hover 光标保持可点击态。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。

### 风险备注

- 当前图标可点击热区等于 `16px` 图标本身；如果后续要兼容更严格的触控命中面积，可再扩展外层透明命中区。

---

## TanStack 空态图片与文案样式

### 范围

- `packages/ui/src/components/table/assets/table-empty-state.webp`
- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 无数据时已改为业务指定图片展示，并默认文案为“暂未生产任何数据”。
- 文案样式已对齐用户指定：`PingFang SC`、`14px`、`400`、`20px`、居中、`letter-spacing: 0`。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。
- `apps/admin`：`build` 通过，空态图片成功打包。

### 风险备注

- 当前空态图片宽高按组件内固定框体比例渲染；若后续更换不同宽高比图片，建议同步调整 `ob-tanstack-table__empty-figure` 尺寸。

---

## TanStack 空态 overlay（居中 + 禁止空态横向滚动）

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 空态从 `tbody` 空行切换为 `table-scroll` 内绝对定位 overlay，图片与文案在可视区域居中展示。
- 空态时滚动容器增加 `is-empty`，横向滚动条被关闭，避免无数据场景出现横向滚动。
- 空态能力位于 `TanStackTable` 组件级，树形表格与普通表格均统一生效。
- 空态图容器改为按组件宽度自适应（不依赖 `100vw`），窄容器下仍保持居中且不溢出。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin build
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`12/12`），`typecheck/lint` 通过。
- `apps/admin`：`build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 空态横向滚动关闭仅在 `tableRows.length === 0` 时生效；有数据时保持原有横向滚动行为与 fixed 列策略。

---

## TanStack 性能优化（树数据同步 + 布局收口）

### 范围

- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`
- `packages/ui/src/components/table/assets/table-empty-state.webp`

### 结论

- 树数据同步仅在 `treeConfig.lazy + loadMethod` 时深拷贝；非 lazy 场景直接浅同步数据源，并将 watch 深度降为 `deep: false`。
- `tableLayout` 支持在“虚拟滚动 / 手动列宽 / 显式列宽”场景自动收口到 `fixed`，降低列宽与大表渲染时布局抖动。
- 扩展状态与跨页选择同步补充了空态短路，减少无效计算。
- 用户确认保留空态图片资源差异并一并纳入本次提交。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin build
pnpm check:admin:bundle
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`14/14`），`typecheck/lint` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。
- `apps/admin`：`build` 成功；`check:admin:bundle` 全部 PASS。

### 风险备注

- 非 lazy 树场景改为浅同步后，若外部直接原地 mutate 深层子节点，需确保上层触发响应式更新（例如替换数组引用）以驱动视图刷新。

---

## adminManagement/org 灰度：组织管理页切 TanStack

### 范围

- `apps/admin/src/modules/adminManagement/org/list.vue`
- `apps/admin/src/modules/adminManagement/org/list.source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `/system/org` 组织管理页已从 `ObVxeTable` 切换到 `ObTanStackTable`。
- 树形能力保持不变：仍使用 `:tree-config="table.treeConfig"` + `row-key="id"`。
- 页面交互保持一致：操作列插槽与“新增下级组织”等动作链路未变。
- 文档已同步灰度状态，便于后续按模块继续推进替换。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：4 个源码门禁测试通过（`4/4`），`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 当前仅切换 `apps/admin` 组织管理页；`apps/admin-lite` 的组织管理仍为 `ObVxeTable`，若需要统一需补一轮同步替换与回归。

---

## TanStack 表格增强：超长省略 tooltip + 空值占位

### 范围

- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/components/table/types.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 新增空值展示能力：默认空值展示 `---`，可通过 `showEmptyValue` 关闭，或用 `emptyValueText` 自定义。
- 新增空态文案配置：无数据文案从固定文案改为 `emptyText` 可配置，默认仍为“暂未生产任何数据”。
- 超长文本省略稳定性增强：列支持 `showOverflowTooltip/ellipsis` 控制省略与 tooltip，单元格与子节点均补齐省略样式。
- tooltip 与单元格默认渲染的空值文案保持一致，避免标题与显示不一致。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`16/16`），`typecheck/lint` 通过。
- `apps/admin`：`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 目前 tooltip 仍为浏览器原生 `title` 方案；如后续需要复杂富文本或可交互 tooltip，再评估切换到组件级 tooltip。

---

## 组织管理页配置未生效修复（省略/tooltip）

### 范围

- `apps/admin/src/modules/adminManagement/org/list.vue`
- `packages/ui/src/components/table/TanStackTable.vue`
- `packages/ui/src/components/table/internal/tanstack-engine.ts`
- `packages/ui/src/tanstack-table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 组织管理页 `orgName` 插槽已补齐“标签不收缩 + 文本单行省略 + title”策略，插槽列在窄列场景下可稳定省略。
- TanStack 表头单元格已统一单行省略，并支持 `title` 提示，避免列宽压缩时标题换行。
- 引擎层在无显式 `width` 时让 `minWidth` 作为列基础宽度生效，降低“配了 minWidth 但视觉不生效”的概率。
- 文档已同步 `minWidth` 生效口径。

### 验证命令

```bash
pnpm exec vp test run /Users/haoqiuzhi/code/one-base-template/packages/ui/src/tanstack-table-source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/packages/ui lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin test:run:file -- src/modules/adminManagement/org/list.source.test.ts
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/admin typecheck
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs lint
pnpm -C /Users/haoqiuzhi/code/one-base-template/apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`17/17`），`typecheck/lint` 通过。
- `apps/admin`：组织管理源码门禁通过（`1/1`），`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

### 风险备注

- 当前 tooltip 仍为原生 `title`，已满足文本提示诉求；若后续需要富内容 tooltip，再统一切到组件级 tooltip。

---

## Element Table 收口：删除 TanStack 并完成 5 页回切

### 范围

- `packages/ui/src/components/table/ElementTable.vue`
- `packages/ui/src/index.ts`
- `packages/ui/src/plugin.ts`
- `packages/ui/src/element-table-source.test.ts`
- `apps/admin/src/modules/LogManagement/login-log/list.vue`
- `apps/admin/src/modules/adminManagement/menu/list.vue`
- `apps/admin/src/modules/adminManagement/org/list.vue`
- `apps/admin/src/modules/adminManagement/role/list.vue`
- `apps/admin/src/modules/adminManagement/role-assign/list.vue`
- `apps/docs/docs/guide/table-vxe-migration.md`
- `packages/ui/AGENTS.md`
- `apps/admin/AGENTS.md`

### 结论

- `packages/ui` 已完成 TanStack 表格链路回收，默认新增 `ObElementTable` / `ElementTable` 作为基于 `Element Plus` 的统一表格壳。
- TanStack 组件、内部引擎、SVG 资产、源码测试和锁文件依赖已删除；仓库运行时不再依赖 `@tanstack/*`。
- 5 个已灰度页面已统一切换到 `ObElementTable`，页面源码门禁全部通过。
- 文档与规则已同步收口为：页面层禁止直接使用 `<el-table>`，统一使用 `ObVxeTable` 或 `ObElementTable`。

### 验证命令

```bash
pnpm -C packages/ui typecheck
pnpm exec vp test run packages/ui/src/element-table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm install --lockfile-only
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁测试通过（`3 files / 8 tests`），`typecheck/lint/build` 通过。
- `apps/admin`：5 个页面源码门禁测试通过（`5/5`），`typecheck/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。
- `pnpm-lock.yaml`：已更新；运行态代码与锁文件不再包含 `@tanstack/*` 依赖。

### 风险备注

- 当前仓库仍保留历史设计/计划文档中关于 TanStack 的说明，用于追溯这轮回收背景；它们不再代表现行实现。
