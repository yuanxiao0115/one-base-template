# 2026-04-01 验证记录

## Portal 老组件迁移（pb 系列 9 个组件）

### 范围

- `packages/portal-engine/src/materials/cms/publicity-education/**`
- `packages/portal-engine/src/materials/cms/mail-list/**`
- `packages/portal-engine/src/materials/cms/dept-upload-files/**`
- `packages/portal-engine/src/registry/materials-registry.ts`
- `packages/portal-engine/src/materials/static-fallbacks/index-fallbacks.ts`
- `packages/portal-engine/src/materials/static-fallbacks/content-fallbacks.ts`
- `packages/portal-engine/src/materials/static-fallbacks/style-fallbacks.ts`
- `packages/portal-engine/src/materials/useRendererMaterials.test.ts`
- `packages/portal-engine/src/registry/materials-registry.aliases.test.ts`
- `apps/docs/docs/guide/portal/material-extension.md`

### 结论

- 已补齐 `publicity-education`、`mail-list`、`dept-upload-files` 三个缺失物料，并接入统一容器和公共配置链路。
- 已完成 `pb-*` 存量兼容：
  - 类型层：`pb-*` -> `basic/cms-*` 映射；
  - 组件名层：`pb-*-index/content/style` 别名映射。
- 已覆盖本轮 9 个迁移组件（含已存在但命名变更的 6 个组件）并补充自动化断言。

### 验证命令

```bash
pnpm -C packages/portal-engine typecheck
pnpm -C packages/portal-engine test:run src/materials/useRendererMaterials.test.ts src/registry/materials-registry.aliases.test.ts
pnpm -C packages/portal-engine lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/portal-engine`：`typecheck` 通过。
- `packages/portal-engine`：2 个测试文件 `5/5` 通过。
- `packages/portal-engine`：`lint` 0 warning / 0 error。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## Portal 业务跳转与页面落位规则落盘

### 范围

- `packages/portal-engine/AGENTS.md`
- `apps/docs/docs/guide/portal/material-extension.md`

### 结论

- 已明确并落盘：业务物料仅声明 `target/action`，禁止写死业务路由；具体 `target -> route` 映射由应用层维护。
- 已明确并落盘：通讯录/应用中心等业务页面必须位于 `apps/*/src/modules/**/pages`，`portal-engine` 不承载业务页面实现。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## Portal 页面设置（Banner 与内容区间距支持负值）

### 范围

- `packages/portal-engine/src/schema/page-settings.ts`
- `packages/portal-engine/src/workbench/page-settings/PortalPageSettingsForm.vue`
- `packages/portal-engine/src/renderer/PortalPreviewPanel.vue`
- `packages/portal-engine/src/schema/page-settings.test.ts`
- `packages/portal-engine/src/renderer/PortalPreviewPanel.test.ts`
- `apps/docs/docs/guide/portal/admin-designer.md`

### 结论

- 页面设置协议新增 `settings.banner.contentSpacing`（默认 `12`，允许 `[-400, 600]`）。
- Banner 开启后可直接在页面设置中配置“内容区与 Banner 间距（px，可负数）”。
- 预览渲染由固定 `gap` 改为读取 `contentSpacing`；配置负值时内容区会向上覆盖 Banner，实现叠压效果。

### 验证命令

```bash
pnpm -C packages/portal-engine test:run src/schema/page-settings.test.ts src/renderer/PortalPreviewPanel.test.ts
pnpm -C packages/portal-engine typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/portal-engine`：2 个测试文件 `10/10` 通过；`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## 菜单管理改造闭环（系统分栏 + 树形上级 + 顶级系统约束）

### 范围

- `apps/admin/src/modules/adminManagement/menu/components/MenuPermissionEditForm.vue`
- `apps/admin/src/modules/adminManagement/menu/composables/useMenuManagementPageState.ts`
- `apps/admin/src/modules/adminManagement/menu/list.vue`
- `apps/admin/src/modules/adminManagement/menu/form.ts`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/module-system.md`

### 结论

- 菜单管理页已改为“左系统列表 + 右侧权限树”双栏交互；右侧只展示当前系统下的子菜单树。
- 菜单编辑表单上级权限已改为树形选择，且权限类型规则收敛为：顶级仅系统、子级禁系统。
- 非菜单或外链场景不再展示“组件路径”字段，减少无效输入。
- `el-tree-select` 的类型阻塞已修复：移除 `TreeOptionProps` 不支持的 `value` 映射键后，`apps/admin typecheck` 通过。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：菜单源码测试 `3/3` 通过；`typecheck` 通过；`build` 通过。
- `apps/admin`：`lint` 为 `0 error`，存在 `4` 条 `max-lines` warning（含 `useMenuManagementPageState.ts`）。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## 菜单管理补充（permission/tree 全量树复用）

### 范围

- `apps/admin/src/modules/adminManagement/menu/composables/useMenuManagementPageState.ts`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`

### 结论

- 新增 `ensurePermissionTreeLoaded()`：当 `permissionTree` 已加载时直接复用，不重复请求 `/permission/tree`。
- `loadParentOptions()` 已改为“优先复用树缓存 + 首次兜底请求”，满足菜单编辑弹窗场景下的减少重复请求目标。
- 左侧系统切换已改为“本地切片刷新 `dataList`”，不再触发 `onSearch` 重新请求整棵树。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts
pnpm -C apps/admin typecheck
```

### 验证结果

- 菜单源码测试 `3/3` 通过。
- `typecheck` 通过。

## 菜单管理补充（系统与权限拆分表单 + 左侧新增系统入口）

### 范围

- `apps/admin/src/modules/adminManagement/menu/components/SystemPermissionEditForm.vue`
- `apps/admin/src/modules/adminManagement/menu/list.vue`
- `apps/admin/src/modules/adminManagement/menu/composables/useMenuManagementPageState.ts`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/module-system.md`

### 结论

- 新增系统入口已迁移到左侧系统列表头部右上角（`+ 新增系统`），不再与右侧权限按钮混在一起。
- 系统场景改为独立表单组件：上级固定顶级、权限类型固定系统，并移除“打开方式/图片地址/缓存路由/跳转地址”。
- 抽屉规格已按场景切换：系统场景单列 `400px`，权限场景维持原多字段布局。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：菜单源码测试 `3/3` 通过；`typecheck` 通过。
- `apps/admin`：`lint` 为 `0 error`，有既有 `4` 条 `max-lines` warning。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## 菜单管理补充（系统列表编辑/删除 icon）

### 范围

- `apps/admin/src/modules/adminManagement/menu/list.vue`
- `apps/admin/src/modules/adminManagement/menu/composables/useMenuManagementPageState.ts`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/module-system.md`

### 结论

- 左侧系统列表支持 icon 级“编辑/删除”操作，点击 icon 不会触发系统切换。
- 系统项操作 icon 规格统一为 `16x16`，图标组间距统一 `8px`。
- 交互语义与右侧操作列保持一致，系统运维不再依赖切换到右侧区域。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：菜单源码测试 `3/3` 通过；`typecheck` 通过。
- `apps/admin`：`lint` 为 `0 error`，存在既有 `4` 条 `max-lines` warning。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## ObTable 性能第二批（列签名监听 + 树归一化引用复用 + tableKey 告警）

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/internal/table-helpers.ts`
- `packages/ui/src/components/table/internal/table-helpers.test.ts`
- `packages/ui/src/table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `Table.vue` 的列监听已由 `deep watch props.columns` 改为 `columnsLayoutSignature`，减少深层响应式遍历。
- 同页多表 `tableKey` 冲突时，`ObTable` 会在开发阶段输出一次告警，降低排查成本。
- `normalizeTreeRows` 现为“结构共享”策略：未变化树节点复用原引用，仅在字段修正时返回新引用，降低大树表场景对象重建开销。
- 文档已同步更新 `tableKey` 告警与树归一化引用复用行为说明。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/components/table/internal/table-helpers.test.ts packages/ui/src/table-source.test.ts packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts packages/ui/src/components/table/internal/table-helpers.test.ts packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：6 个测试文件 `18/18` 通过；`typecheck/lint/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## ObTable 性能优化（watch 调度合并 + fixHeader 回收）

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/internal/use-table-row-drag-sort.ts`
- `packages/ui/src/components/table/internal/use-table-layout.ts`
- `packages/ui/src/components/table/internal/use-table-layout.test.ts`
- `packages/ui/src/table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `Table.vue` 已把多路 `watch` 的重排与拖拽初始化收敛为调度函数，减少重复 `doLayout` 与 `initSortable` 调用。
- `use-table-row-drag-sort.ts` 已把 `enabled/data/config` 变更合并为单任务调度，降低连续状态变更时的重复初始化开销。
- `use-table-layout.ts` 已支持 sticky 样式回收：`adaptive` 关闭或 `fixHeader=false` 时会移除表头 `position/top/z-index` 行内样式。
- 文档已补充 `fixHeader` 关闭场景的行为说明。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/table-source.test.ts
pnpm exec vp test run packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/table-source.test.ts packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：5 个测试文件 `16/16` 通过；`typecheck/lint/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## puretable fork 第二批（tableKey / adaptive / pagination）

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/internal/table-helpers.ts`
- `packages/ui/src/table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTable` 已完成 `tableKey` registry 语义收口：默认 key 改为组件实例 `uid` 派生值，`props.tableKey` 可覆盖，`getTableRef()` 按 key 获取实例，避免同页多表默认 key 冲突。
- `adaptive` 已按 pure 预期切到“视口贴底优先 + 容器高度兜底”，并补齐 `ResizeObserver + window.resize` 监听与清理。
- `getTableDoms()` / `setHeaderSticky()` 已增强容错：优先 `$el` 根节点 + 多 selector fallback + fixed header 批量 sticky。
- 分页已区分 controlled 与 default 语义：受控 `currentPage/pageSize` 与 `defaultCurrentPage/defaultPageSize` 不再混用；`total > 0` 时不再透传 `pageCount`。
- 文档与源码门禁测试已同步更新。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts src/modules/LogManagement/sys-log/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：3 个测试文件 `12/12` 通过；`typecheck`、`lint`、`build` 通过。
- `apps/admin`：6 个源码测试文件 `8/8` 通过；`typecheck` 与 `build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 风险备注

- monitor 子代理复核结论：无 P0/P1。
- 当前保留 1 个可接受 P2：`getTableDoms/setHeaderSticky` 仍依赖 Element Plus 内部 DOM class，后续升级 Element Plus 版本时需重点回归 `adaptive + fixHeader + fixed columns` 组合场景。

## multi-role 走查后补强（expandSlot / formatter / lazy-tree）

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/internal/table-helpers.ts`
- `packages/ui/src/table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `expandSlot` 语义已补强：`type='expand'` 支持 `expandSlot` 与 `slots.expand` 双链路，避免误落普通字段渲染。
- `formatter` 语义已补齐：支持 pure 4 参签名与 params 签名，修复类型契约有定义但运行时未生效的问题。
- lazy tree `load` 返回值已统一经过 `normalizeTreeRows`，修复首层与子层归一化规则不一致。
- 默认单元格渲染已覆盖 boolean/bigint 文本化，避免布尔值显示为空白。
- 风险清单已写入文档，含 DOM 结构耦合、性能热点和列拖拽能力边界。

### 验证结果

- `packages/ui`：源码测试 `12/12` 通过；`typecheck/lint/build` 通过。
- `apps/admin`：6 个源码测试文件 `8/8` 通过；`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 仍需后续优化（非阻断）

- tooltip 批量实例在大表格场景的渲染开销（性能走查 P1）。
- adaptive 在高频 resize 场景下的观察器重绑与 `doLayout` 热点（性能走查 P1）。
- 统一壳组件暂未形成“列拖拽排序”标准能力（`ObTable` 不支持，`ObVxeTable` 仅透传扩展入口）。

## 移除 pure 命名，统一表格契约命名体系

### 范围

- `packages/ui/src/components/table/table-contract/column-contract.ts`
- `packages/ui/src/components/table/types.ts`
- `packages/ui/src/table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 表格列契约文件已迁移到 `table-contract` 目录，运行时代码与测试不再引用 `puretable-fork`。
- 原 `Pure*` 类型与接口已统一改为 `ObTable*` 命名，`packages/ui/src/components/table/types.ts` 已同步切换到新契约。
- `packages/ui/src/table-source.test.ts` 已改为断言新路径与 `ObTableColumnsContract`，并顺手修正现有 `wrapperClass` 源码断言，保证用户指定验证命令可通过。
- 迁移文档已改为中性命名，不再使用 `puretable-fork` 路径和 `pure` 命名表述描述当前实现。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：3 个测试文件 `12/12` 通过。
- `packages/ui`：`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 风险备注

- 当前只完成命名与路径收口，未改动 `pnpm mirror:pure-table` 等上游镜像命令；若后续要彻底去除历史来源命名，需要单独评估镜像脚本与命令别名影响。

## ObTable 行拖拽 + 布局拆分 + 关系说明补齐

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/Table.css`
- `packages/ui/src/components/table/types.ts`
- `packages/ui/src/components/table/internal/use-table-layout.ts`
- `packages/ui/src/components/table/internal/use-table-row-drag-sort.ts`
- `packages/ui/src/table-source.test.ts`
- `packages/ui/src/index.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTable` 已支持行拖拽排序契约：`rowDrag`、`rowDragConfig`、`row-drag-sort`。
- `Table.vue` 已进一步拆分：自适应高度与 DOM 探测逻辑迁移到 `use-table-layout.ts`，降低单文件复杂度并减少观察器重复绑定。
- 自适应链路改为“仅调度重算 + 高度变化才触发 doLayout”，高频 resize 下渲染抖动风险下降。
- 文档已补齐 `ObTable` 与 `el-table` 的封装关系、版本解析关系（`packages/ui` peerDependencies + app 侧统一版本解析）。
- 风险清单已补充：`expandSlot` 深度组合回归提示、`getTableDoms/setHeaderSticky` DOM 结构耦合提示、列拖拽未标准化提示。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts src/modules/LogManagement/sys-log/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：3 个源码门禁文件 `12/12` 通过；`typecheck/lint/build` 通过。
- `apps/admin`：6 个源码门禁文件 `8/8` 通过；`typecheck/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 风险备注

- 行拖拽当前为“行级排序”能力，列拖拽排序仍未组件化；若业务要列拖拽，仍建议先做页面级 PoC 并评估持久化策略。
- `getTableDoms/setHeaderSticky` 仍依赖 Element Plus 内部 class，升级 Element Plus 时需回归 `adaptive + fixHeader + fixed columns + rowDrag`。

## 走查问题修复（重复初始化 / 宽度重排 / 依赖契约）

### 范围

- `packages/ui/src/components/table/internal/use-table-layout.ts`
- `packages/ui/src/components/table/internal/use-table-row-drag-sort.ts`
- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/components/table/Table.css`
- `packages/ui/package.json`
- `pnpm-lock.yaml`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 已修复 `adaptive` 的宽度变化重排风险：`ResizeObserver/window.resize` 触发时强制 `doLayout()`，避免高度不变但列宽错位。
- 已修复 rowDrag 重复初始化热点：增加 `tbody + config` 签名缓存，减少重复 destroy/create。
- 已补齐 rowDrag 依赖契约：`packages/ui` 明确声明 `sortablejs`，并在缺失时输出一次告警。
- 已补充无障碍增强：稳定 region 的 `aria-busy`、`aria-live` 状态播报、键盘 `Alt + ↑ / ↓` 行顺序调整。
- 文档已同步更新依赖与键盘交互说明。

### 验证命令

```bash
pnpm install --lockfile-only
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/admin test:run:file -- src/modules/LogManagement/login-log/list.source.test.ts src/modules/LogManagement/sys-log/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁 `13/13` 通过；`typecheck/lint/build` 通过。
- `apps/admin`：6 个源码门禁文件 `8/8` 通过；`typecheck/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 风险备注

- `getTableDoms/setHeaderSticky` 对 Element Plus 内部 class 的耦合仍是长期维护点。
- 列拖拽排序仍未标准化（本次仅收口行拖拽排序）。

## tooltip 默认轻量化（性能收口）

### 范围

- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/table-source.test.ts`
- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- `ObTable` 默认关闭富 `ElTooltip`（`tooltipRenderThreshold=0`），改为轻量 `title` 提示。
- 保留阈值开关：当 `tooltipRenderThreshold > 0` 时可按行数启用富 tooltip。
- 在不改变 `showOverflowTooltip` 语义的前提下，默认渲染链路的组件实例开销显著下降。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：源码门禁 `13/13` 通过；`typecheck/lint/build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## 范围确认（不做列拖拽排序）

### 范围

- `apps/docs/docs/guide/table-vxe-migration.md`

### 结论

- 已按用户确认收口：当前阶段不规划列拖拽排序能力。
- 表格拖拽能力边界固定为“仅行拖拽（含键盘辅助）”。

## 行为级测试补齐（行拖拽 + 自适应）

### 范围

- `packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts`
- `packages/ui/src/components/table/internal/use-table-layout.test.ts`

### 结论

- 已新增行拖拽行为测试，覆盖“相同 `tbody + config` 场景不重复创建 Sortable 实例”。
- 已新增键盘交互测试，覆盖 `Alt + ArrowUp/ArrowDown` 触发 `row-drag-sort` 语义。
- 已新增布局行为测试，覆盖“高度不变时 `forceLayout` 仍触发 `doLayout`”，锁定宽度重排修复点。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
```

### 验证结果

- `packages/ui`：`5 files / 15 tests` 通过。
- `packages/ui`：`typecheck/lint/build` 通过。

## ObTable 列桥接拆分（Table.vue 减负）

### 范围

- `packages/ui/src/components/table/internal/use-table-column-bridge.ts`
- `packages/ui/src/components/table/Table.vue`
- `packages/ui/src/table-source.test.ts`

### 结论

- 已将列桥接/列渲染主链路从 `Table.vue` 抽离到 `use-table-column-bridge.ts`，保留既有外部 props/emits/expose 契约不变。
- `Table.vue` 主文件规模由 `1268` 行降到 `910` 行，职责收敛为布局、分页、树表与拖拽编排。
- 源码门禁已同步到新模块，`filterIconSlot/expandSlot/formatter` 等关键语义仍受测试保护。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/components/table/internal/use-table-row-drag-sort.test.ts packages/ui/src/components/table/internal/use-table-layout.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C packages/ui build
```

### 验证结果

- `packages/ui`：`5 files / 15 tests` 通过。
- `packages/ui`：`typecheck/lint/build` 通过。

## adminManagement：user 跨页勾选 + org 懒加载树展开修复

### 范围

- `apps/admin/src/modules/adminManagement/user/composables/useUserCrudState.ts`
- `apps/admin/src/modules/adminManagement/user/composables/useUserStatusActions.ts`
- `apps/admin/src/modules/adminManagement/user/columns.tsx`
- `apps/admin/src/modules/adminManagement/org/api.ts`
- `apps/admin/src/modules/adminManagement/user/composables/useUserCrudState.unit.test.ts`
- `apps/admin/src/modules/adminManagement/org/api.source.test.ts`
- `apps/docs/docs/guide/crud-module-best-practice.md`

### 结论

- 用户管理跨页勾选已收敛到页面编排状态（`useUserCrudState`）：支持跨页累积、翻页回显、查询条件切换清空。
- 批量启停已消费跨页选中集合，不再仅限当前页选中。
- 组织管理 `getOrgTree` 已补 `hasChildren` 回退，懒加载树表可展示展开入口，行为对齐老项目。
- 文档已同步补充“跨页勾选落层原则 + 树表展开回退策略”。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- src/modules/adminManagement/user/composables/useUserCrudState.unit.test.ts src/modules/adminManagement/user/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/org/api.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：定向测试 `9/9` 通过；`typecheck`、`build` 通过。
- `apps/admin lint`：0 error（存在 2 条历史 `max-lines` warning，均为既有文件体量告警）。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## adminManagement/org：树展开按钮并排样式优化

### 范围

- `apps/admin/src/modules/adminManagement/org/columns.tsx`
- `apps/admin/src/modules/adminManagement/org/list.vue`

### 结论

- 组织全称列树节点布局已调整为并排显示，“展开按钮 + 标签 + 名称”保持同一行。
- 展开按钮视觉已收敛（尺寸、圆角、hover/展开态），避免原先按钮位置与文本错位感。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- src/modules/adminManagement/org/list.source.test.ts
pnpm -C apps/admin typecheck
```

### 验证结果

- `apps/admin`：组织管理定向源码测试 `3/3` 通过。
- `apps/admin`：`typecheck` 通过。

## adminManagement 全模块 Table 统一为 ObTable（含 skill/agent 更新）

### 范围

- `apps/admin/src/modules/adminManagement/position/list.vue`
- `apps/admin/src/modules/adminManagement/tenant-info/list.vue`
- `apps/admin/src/modules/adminManagement/tenant-manager/list.vue`
- `apps/admin/src/modules/adminManagement/org/components/OrgLevelManageDialog.vue`
- `apps/admin/src/modules/adminManagement/position/list.source.test.ts`
- `apps/admin/src/modules/adminManagement/tenant-info/list.source.test.ts`
- `apps/admin/src/modules/adminManagement/tenant-manager/list.source.test.ts`
- `apps/admin/src/modules/adminManagement/org/components/OrgLevelManageDialog.source.test.ts`
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/admin-agent-redlines.md`
- `apps/docs/docs/guide/crud-module-best-practice.md`
- `.codex/skills/admin-management-standardizer/references/checklists.md`
- `.codex/skills/crud-module-best-practice/SKILL.md`
- `.codex/skills/crud-module-best-practice/references/position-crud-blueprint.md`

### 结论

- `adminManagement` 下所有当前模块列表/弹窗表格编排已统一为 `ObTable`，业务源码已无 `ObVxeTable` 实例。
- 已新增 4 个源码门禁测试，确保后续回归时不会重新引入 `ObVxeTable`。
- 已同步更新 admin 规则、docs 指南与技能模板，明确 `adminManagement` 强制 `ObTable`。

### 关键核查

```bash
rg -n "<ObVxeTable|ObVxeTable" apps/admin/src/modules/adminManagement
```

核查结果：仅剩 source test 中 `not.toContain('<ObVxeTable')` 的反向断言，业务文件无残留。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- src/modules/adminManagement/user/list.source.test.ts src/modules/adminManagement/org/list.source.test.ts src/modules/adminManagement/role/list.source.test.ts src/modules/adminManagement/role-assign/list.source.test.ts src/modules/adminManagement/menu/list.source.test.ts src/modules/adminManagement/position/list.source.test.ts src/modules/adminManagement/tenant-info/list.source.test.ts src/modules/adminManagement/tenant-manager/list.source.test.ts src/modules/adminManagement/org/components/OrgLevelManageDialog.source.test.ts
pnpm -C apps/admin lint:arch
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：`9 files / 12 tests` 通过。
- `apps/admin`：`lint:arch/typecheck/lint/build` 通过。
- `apps/admin lint`：0 error（2 条历史 `max-lines` warning，非本次新增）。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

## admin 全模块 ObVxeTable 替换为 ObTable

### 范围

- `apps/admin/src/modules/CmsManagement/audit/components/ArticleAuditPanel.vue`
- `apps/admin/src/modules/CmsManagement/audit/components/CommentAuditPanel.vue`
- `apps/admin/src/modules/CmsManagement/column/list.vue`
- `apps/admin/src/modules/CmsManagement/content/list.vue`
- `apps/admin/src/modules/SystemManagement/dict/list.vue`
- `apps/admin/src/modules/PortalManagement/templatePage/list.vue`
- `apps/admin/src/modules/adminManagement/org/components/OrgLevelManageDialog.vue`
- `apps/admin/src/modules/adminManagement/user/composables/useUserDragSort.ts`
- `apps/admin/src/modules/CmsManagement/audit/components/ArticleAuditPanel.source.test.ts`
- `apps/admin/src/modules/CmsManagement/audit/components/CommentAuditPanel.source.test.ts`
- `apps/admin/src/modules/CmsManagement/column/list.source.test.ts`
- `apps/admin/src/modules/CmsManagement/content/list.source.test.ts`
- `apps/admin/src/modules/SystemManagement/dict/list.source.test.ts`
- `apps/admin/src/modules/PortalManagement/templatePage/list.source.test.ts`
- `apps/admin/src/modules/adminManagement/user/composables/useUserDragSort.source.test.ts`
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/admin-agent-redlines.md`
- `apps/docs/docs/guide/crud-module-best-practice.md`
- `.codex/skills/admin-management-standardizer/references/checklists.md`
- `.codex/skills/crud-module-best-practice/SKILL.md`
- `.codex/skills/crud-module-best-practice/references/position-crud-blueprint.md`

### 结论

- admin 模块业务源码中的 `<ObVxeTable>` 已全部替换为 `<ObTable>`。
- admin 业务源码中的 `.ob-vxe-table`、`.vxe-table--*` 残留已清理。
- 规则与技能文档已同步更新为“admin 模块统一 ObTable”。

### 关键核查

```bash
rg -n "ObVxeTable|ob-vxe-table|vxe-table--" apps/admin --glob '!**/dist/**'
```

核查结果：仅剩 `apps/admin/AGENTS.md` 的禁用规则文案与 source test 反向断言，业务代码无残留。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- $(rg --files apps/admin/src/modules | rg 'source\.test\.ts$' | sed 's#apps/admin/##')
pnpm -C apps/admin lint:arch
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：`20 files / 23 tests` 通过。
- `apps/admin`：`lint:arch/typecheck/lint/build` 通过。
- `apps/admin lint`：0 error（2 条历史 `max-lines` warning，非本次新增）。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

## admin 构建去除 vxe 运行时 chunk

### 范围

- `packages/ui/src/plugin-obtable.ts`
- `packages/ui/src/obtable.ts`
- `packages/ui/src/vxe.ts`
- `packages/ui/src/index.ts`
- `packages/ui/src/components/table/CardTable.vue`
- `packages/ui/src/components/table/VxeTable.vue`
- `packages/ui/package.json`
- `packages/ui/src/card-table-source.test.ts`
- `apps/admin/src/bootstrap/plugins.ts`
- `apps/admin/tests/architecture/obtable-plugin-source.unit.test.ts`
- `apps/admin-lite/src/bootstrap/plugins.ts`
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/admin-agent-redlines.md`
- `apps/docs/docs/guide/crud-module-best-practice.md`
- `.codex/skills/admin-management-standardizer/references/checklists.md`
- `.codex/skills/crud-module-best-practice/SKILL.md`

### 结论

- admin 启动层已切换到 `@one-base-template/ui/obtable`，避免引入 legacy `VxeTable` 注册链路。
- `CardTable` 分页已由 `VxePager` 收口到 `el-pagination`，admin 侧不再通过卡片列表引入 vxe 依赖。
- `apps/admin build` 产物中 `assets/vxe-*` 已消失。

### 关键核查

```bash
ls apps/admin/dist/assets | rg '^vxe-|vxe'
node scripts/check-admin-build-size.mjs
```

核查结果：

- `ls ... | rg` 无输出（未生成 `vxe` 产物）。
- `check-admin-build-size` 显示：`vxe chunk: 未匹配到对应 chunk，跳过。`。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/table-source.test.ts packages/ui/src/card-table-source.test.ts packages/ui/src/vxe-table-source.test.ts packages/ui/src/index.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C apps/admin test:run:file -- tests/architecture/obtable-plugin-source.unit.test.ts $(rg --files apps/admin/src/modules | rg 'source\.test\.ts$' | sed 's#apps/admin/##')
pnpm -C apps/admin lint:arch
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin build
node scripts/check-admin-build-size.mjs
pnpm -C apps/admin-lite typecheck
pnpm -C apps/admin-lite lint:arch
```

### 验证结果

- `packages/ui`：`5 files / 16 tests` 通过，`typecheck/lint` 通过。
- `apps/admin`：`21 files / 24 tests` 通过，`lint:arch/typecheck/lint/build` 通过。
- `apps/admin build`：无 `vxe` js/css 产物。
- `check-admin-build-size`：`vxe chunk` 未匹配，其余预算项 PASS。
- `apps/admin-lite`：`typecheck/lint:arch` 通过。
- `apps/admin lint`：0 error（2 条历史 `max-lines` warning，非本次新增）。

## adminManagement 多角色走查整改 + 测试目录迁移

### 范围

- `apps/admin/src/modules/adminManagement/org/api.ts`
- `apps/admin/src/modules/adminManagement/org/composables/useOrgPageState.ts`
- `apps/admin/src/modules/adminManagement/org/composables/useOrgTreeQuery.ts`
- `apps/admin/src/modules/adminManagement/org/components/OrgLevelManageDialog.vue`
- `apps/admin/tests/modules/adminManagement/**`（由 `src/modules/adminManagement/**` 迁移）
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/admin-agent-redlines.md`
- `apps/admin/src/types/components.d.ts`

### 结论

- `org/api.ts` 已恢复“接口透传”职责，不再做树数据归一化。
- 树表 `hasChildren` 补值逻辑已下沉到页面/查询 composable，满足“单一边界转换”。
- `OrgLevelManageDialog` 模板不再直接调用 `crud.open*`，改为 handler 收口。
- `adminManagement` 测试文件已全部迁移到 `apps/admin/tests/modules/adminManagement/**`，业务目录无测试文件残留。
- source test 已补齐 `treeNode: true` 红线断言。
- 按用户确认，`components.d.ts` 本次一并提交。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- $(rg --files apps/admin/tests/modules/adminManagement | rg '(source|unit)\.test\.ts$' | sed 's#apps/admin/##')
pnpm -C apps/admin lint:arch
pnpm -C apps/admin typecheck
pnpm -C apps/admin lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：`29 files / 61 tests` 通过。
- `apps/admin`：`lint:arch/typecheck` 通过。
- `apps/admin lint`：0 error（3 条历史 `max-lines` warning）。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

## ObTable 入口样式导入修复

### 范围

- `packages/ui/src/obtable.ts`
- `packages/ui/src/index.ts`
- `packages/ui/src/index.test.ts`
- `apps/admin/src/types/auto-imports.d.ts`
- `apps/admin/src/types/components.d.ts`

### 结论

- `@one-base-template/ui/obtable` 子入口已补齐 `table-theme.css` 导入，`ObTable` 的主题变量可正常注入。
- 根入口 `@one-base-template/ui` 同步补齐 `table-theme.css`，避免入口行为分叉。
- 已新增 source test 断言 obtable 子入口必须导入图标与表格主题样式。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/index.test.ts packages/ui/src/table-source.test.ts packages/ui/src/plugin.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C apps/admin build
```

### 验证结果

- `packages/ui`：`3 files / 14 tests` 通过，`typecheck/lint` 通过。
- `apps/admin build`：通过。

## adminManagement shared 分层收敛（cachedAsyncLoader 下沉）

### 范围

- `apps/admin/src/modules/adminManagement/user/composables/useUserRemoteOptions.ts`
- `apps/admin/src/modules/adminManagement/user/utils/cachedAsyncLoader.ts`
- `apps/admin/src/modules/adminManagement/shared/cachedAsyncLoader.ts`（删除）
- `apps/admin/tests/modules/adminManagement/user/utils/cachedAsyncLoader.unit.test.ts`
- `apps/admin/tests/modules/adminManagement/shared/cachedAsyncLoader.unit.test.ts`（删除）
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/admin-agent-redlines.md`

### 结论

- `cachedAsyncLoader` 仅被 `user` 子模块使用，已按分层规则下沉到模块内 `utils`，避免滞留在 `adminManagement/shared`。
- adminManagement 工具分层规则已写入 `AGENTS` 与 docs 红线，后续可按“单模块就近、域内共享、应用级上提”统一执行。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/user/utils/cachedAsyncLoader.unit.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：`1 file / 3 tests` 通过；`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

## PersonalizationDrawer 改造为 CrudContainer 抽屉

### 范围

- `packages/ui/src/components/theme/PersonalizationDrawer.vue`

### 结论

- 组件已从 `el-drawer` 直连改为 `CrudContainer`，并固定抽屉宽度 `400px`。
- 抽屉保留无底部按钮，主题切换内容区保持原有布局语义。

### 验证命令

```bash
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
```

### 验证结果

- `packages/ui`：`typecheck` 通过；`lint` 0 warning / 0 error。

## admin 路由 keepAlive 页面持久化修复（route.name 稳定命中）

### 范围

- `packages/ui/src/components/view/KeepAliveView.vue`
- `packages/ui/src/keep-alive-view-source.test.ts`
- `apps/admin/AGENTS.md`
- `apps/docs/docs/guide/layout-menu.md`

### 结论

- admin 已具备 keepAlive 能力：路由层通过 `meta.keepAlive=true` 声明是否缓存。
- 本次修复后，`KeepAliveView` 会将匹配缓存的页面按 `route.name` 包装为同名组件，确保 `keep-alive include` 命中不再依赖业务页组件名与路由名一致。
- keepAlive 生效口径明确为“`meta.keepAlive` + 稳定唯一 `route.name`”。

### 验证命令

```bash
pnpm exec vp test run packages/ui/src/keep-alive-view-source.test.ts
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/ui`：`1 file / 2 tests` 通过。
- `packages/ui`：`typecheck` 通过，`lint` 0 warning / 0 error。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

## 菜单内嵌闭环（ext/micro 宿主路由 + openMode/redirect 生效 + 表单提示）

### 范围

- `apps/admin/src/modules/adminManagement/routes.ts`
- `apps/admin/src/modules/adminManagement/menu/pages/ExternalFramePage.vue`
- `apps/admin/src/modules/adminManagement/menu/pages/MicroAppHostPage.vue`
- `apps/admin/src/modules/adminManagement/menu/components/MenuPermissionEditForm.vue`
- `packages/core/src/adapter/types.ts`
- `packages/adapters/src/basicAdapter.ts`
- `packages/core/src/stores/menu.ts`
- `packages/ui/src/components/menu/SidebarMenu.vue`
- `packages/ui/src/components/menu/TopMenu.vue`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`
- `apps/admin/tests/modules/adminManagement/menu/embedded-host-routes.source.test.ts`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/menu-route-spec.md`

### 结论

- 已完成“一次改代码，后续只配菜单”的宿主能力：layout 下新增 `/ext/:slug(.*)*` 与 `/micro/:slug(.*)*` 两个通用路由。
- 菜单运行时已打通 `openMode + redirect`：
  - `openMode=1` 时按外部链接处理；
  - 菜单点击外部跳转优先使用 `redirect`，无 `redirect` 回退 `path`。
- `basicAdapter` 已透传 `openMode/redirect/remark`，并扩展 `AppMenuItem` 契约。
- 菜单编辑表单已增加内嵌场景提示与示例文案，减少配置误用。
- docs 已同步写入配置语义与宿主路由约定。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts tests/modules/adminManagement/menu/embedded-host-routes.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C packages/ui typecheck
pnpm -C packages/core typecheck
pnpm -C packages/adapters typecheck
pnpm -C apps/admin lint
pnpm -C packages/ui lint
pnpm -C packages/core lint
pnpm -C packages/adapters lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：定向源码测试 `2 files / 4 tests` 通过；`typecheck` 通过。
- `packages/ui/core/adapters`：`typecheck` 通过；`lint` 0 warning / 0 error。
- `apps/admin lint`：0 error（4 条历史 `max-lines` warning）。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。
- 追加验证：`pnpm -C apps/admin build`。
- 追加结果：`apps/admin` 构建通过，产物包含 `ExternalFramePage` 与 `MicroAppHostPage` 分包。

## 菜单表单提示样式收口（问号 tooltip）

### 范围

- `apps/admin/src/modules/adminManagement/menu/components/MenuPermissionEditForm.vue`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/menu-route-spec.md`

### 结论

- 交互提示从大块提示区收敛为“label 前问号 tooltip”，覆盖 `访问路径`、`跳转地址`、`打开方式` 三个字段。
- `打开方式` 外部文案已统一为“外部（浏览器新窗口）”。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：`1 file / 3 tests` 通过；`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## 菜单列表列数精简

### 范围

- `apps/admin/src/modules/adminManagement/menu/columns.ts`
- `apps/admin/src/modules/adminManagement/menu/list.vue`
- `apps/admin/tests/modules/adminManagement/menu/list.source.test.ts`
- `apps/docs/docs/guide/layout-menu.md`

### 结论

- 菜单列表列已收敛为：`权限名称 / 排序 / 权限类型 / 访问路径 / 状态 / 操作`。
- 低频列（图标、缓存路由、打开方式、跳转地址、组件、备注）已从列表视图移除。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/modules/adminManagement/menu/list.source.test.ts tests/modules/adminManagement/menu/embedded-host-routes.source.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：`2 files / 4 tests` 通过；`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## PortalManagement 物料分类重划分 + CMS 分类改名

### 范围

- `packages/portal-engine/src/registry/materials-registry.ts`
- `apps/docs/docs/guide/portal/material-extension.md`

### 结论

- 默认物料分类已从“基础 + CMS”调整为：`基础组件 / 容器组件 / 列表组件 / 链接组件 / 业务组件 / 党建风格cms组件`。
- `cms` 分类仅改标题，分类 id 与组内物料保持不变。

### 验证命令

```bash
pnpm -C packages/portal-engine typecheck
pnpm -C packages/portal-engine run test:run -- src/materials/registerMaterialExtensions.test.ts
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/portal-engine`：`typecheck` 通过，测试通过（`32 files / 124 tests`）。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## base-table 中文分页 + tag 列展示

### 范围

- `packages/portal-engine/src/materials/base/base-table/index.vue`
- `packages/portal-engine/src/materials/base/base-table/content.vue`
- `packages/portal-engine/src/materials/base/base-table/config.json`
- `packages/portal-engine/src/materials/base/base-table/source.test.ts`
- `apps/docs/docs/guide/portal/material-extension.md`

### 结论

- `base-table` 分页器改为中文文案（`上一页` / `下一页`），并通过 `el-config-provider + zh-cn` 固定中文 locale。
- 列配置新增 tag 展示能力：`showTag`、`tagBgColor`、`tagTextColor`，并支持按字段 key 动态读取标签背景色/文字色。
- 继续保持 `showRowDivider=false` 时隐藏 `el-table__inner-wrapper::before` 伪元素底线。

### 验证命令

```bash
pnpm -C packages/portal-engine run test:run -- src/materials/base/base-table/source.test.ts
pnpm -C packages/portal-engine typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/portal-engine`：源码测试通过（`33 files / 127 tests`），`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。
