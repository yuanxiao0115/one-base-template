# 2026-04-02 验证记录

## 安全收口：v-html / 外链校验 / 认证回跳 target 公共封装

### 范围

- `packages/core/src/utils/{html.ts,url.ts}`
- `packages/core/src/router/{redirect.ts,initial-path.ts}`
- `packages/core/src/index.ts`
- `packages/core/src/utils/{html.test.ts,url.test.ts}`
- `packages/core/src/router/redirect.test.ts`
- `apps/admin/src/components/rich-text/rich-text-html.ts`
- `apps/admin/src/modules/CmsManagement/content/components/ContentEditForm.vue`
- `apps/admin/src/{pages/login/LoginPage.vue,bootstrap/index.ts,services/auth/auth-scenario-provider.ts}`
- `apps/admin-lite/src/{pages/login/LoginPage.vue,bootstrap/index.ts,services/auth/auth-scenario-provider.ts}`
- `apps/admin/src/modules/adminManagement/menu/pages/{ExternalFramePage,MicroAppHostPage}.vue`
- `apps/admin-lite/src/modules/adminManagement/menu/pages/{ExternalFramePage,MicroAppHostPage}.vue`
- `packages/ui/src/components/menu/{SidebarMenu.vue,TopMenu.vue}`
- `apps/admin/tests/bootstrap/index.unit.test.ts`
- `apps/admin-lite/tests/bootstrap/index.unit.test.ts`
- `apps/docs/docs/guide/{menu-route-spec.md,development.md}`

### 结论

- 已在 core 收口三类公共安全能力：
  - HTML 安全渲染：`sanitizeHtmlContent`；
  - 外链校验与目标解析：`isHttpUrl` / `resolveSafeHttpUrl` / `resolveExternalTargetUrl`；
  - 认证回跳 target 解析：`resolveAuthRedirectTargetFromQuery` / `resolveAuthRedirectTargetFromSearchParams`。
- admin 的 `v-html` 正文预览已统一走净化链路，不再直接渲染后端原始 HTML。
- admin/admin-lite 登录页、路由守卫、SSO 回调均改为复用 core 回跳封装，不再手写 `redirect ?? redirectUrl` 组合逻辑。
- admin/admin-lite 内嵌页与 UI 菜单外跳统一复用 `resolveExternalTargetUrl`，避免重复 `isHttpUrl` 与不安全地址打开。
- docs 已补充“同事如何使用”的统一说明与禁用写法。

### 验证命令

```bash
pnpm -C packages/core test:run src/router/redirect.test.ts src/utils/url.test.ts src/utils/html.test.ts
pnpm -C packages/core typecheck
pnpm -C packages/core lint
pnpm -C apps/admin test:run:file -- tests/bootstrap/index.unit.test.ts tests/services/auth/auth-scenario-provider.unit.test.ts src/components/rich-text/rich-text-html.unit.test.ts
pnpm -C apps/admin-lite test:run:file -- tests/bootstrap/index.unit.test.ts tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin-lite typecheck
pnpm -C packages/ui typecheck
pnpm -C packages/ui lint
pnpm -C apps/admin lint
pnpm -C apps/admin-lite lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/core`：相关测试 `3 files / 10 tests` 通过；`typecheck/lint` 通过。
- `apps/admin`：定向测试 `3 files / 13 tests` 通过；`typecheck` 通过。
- `apps/admin-lite`：定向测试 `2 files / 9 tests` 通过；`typecheck` 通过。
- `packages/ui`：`typecheck/lint` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。
- `apps/admin lint` 仍存在 4 条既有 `max-lines` warning（历史存量，非本次改动引入），无 error。

## admin/admin-lite 命名收口 + new:app preset + route meta 风险门禁

### 范围

- `apps/admin/src/modules/{PortalManagement,DocumentFormManagement}/index.ts`
- `apps/admin/src/config/platform-config.ts`
- `apps/admin/src/router/registry.ts`
- `apps/admin/tests/router/{registry,assemble-routes,route-policy}.unit.test.ts`
- `apps/admin/tests/architecture/route-meta-helper-source.unit.test.ts`
- `apps/admin/tests/config/platform-config.unit.test.ts`
- `apps/admin-lite/tests/router/route-policy.unit.test.ts`
- `scripts/new-app.mjs`
- `scripts/__tests__/new-app.test.mjs`
- `apps/admin-lite/README.md`
- `apps/docs/docs/guide/{admin-lite-base-app,quick-start,for-users,menu-route-spec,module-system}.md`
- `apps/admin/AGENTS.md`

### 结论

- admin 可选模块 `moduleMeta.id` 已统一为 kebab-case（`portal-management`、`document-form-management`）。
- admin 路由注册层新增历史 id 自动映射与告警，旧配置可运行但会提示尽快迁移到新 id。
- `new:app` 支持 `--preset minimal|standard|enterprise`，并可按 preset 自动收口 `enabledModules` 与顶栏能力开关。
- `withCrudStarter` 追加模块逻辑已改为“基于当前配置解析后追加”，不再依赖固定数组文本匹配。
- route meta 风险新增自动化约束：`keepAlive` 路由必须有稳定 `name`；`open` 路由必须 `hiddenTab=true` 且不允许 `activePath`。
- 文档与 AGENTS 口径已同步到最新实现。

### 验证命令

```bash
node --test scripts/__tests__/new-app.test.mjs scripts/__tests__/new-module.test.mjs
pnpm -C apps/admin test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts tests/config/platform-config.unit.test.ts
pnpm -C apps/admin-lite test:run:file -- tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts tests/config/platform-config.unit.test.ts
pnpm -C apps/admin-lite lint:arch
pnpm check:naming
pnpm -C apps/docs lint
pnpm -C apps/docs build
pnpm check:admin:bundle
pnpm check:admin-lite:bundle
```

### 验证结果

- `new-app/new-module` 脚手架测试：`7/7` 通过。
- `apps/admin` 定向测试：`5 files / 13 tests` 通过。
- `apps/admin-lite` 定向测试：`3 files / 7 tests` 通过。
- `apps/admin-lite lint:arch` 通过。
- `check:naming` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。
- `check:admin:bundle` 与 `check:admin-lite:bundle` 均通过（startup js count/gzip/tiny chunk 未超预算）。

## docs：版本治理 SOP 与业务版本矩阵

### 范围

- `apps/docs/docs/guide/package-version-governance.md`
- `apps/docs/docs/guide/business-integration-version-matrix.md`
- `apps/docs/docs/.vitepress/config.ts`
- `apps/docs/docs/guide/index.md`
- `apps/docs/docs/guide/for-maintainers.md`
- `apps/docs/docs/guide/package-release.md`

### 结论

- 已新增“多主线版本治理 SOP”文档，明确 `main` / `release/<major>.x` 的维护口径与发布步骤。
- 已新增“业务接入版本矩阵与迁移模板”文档，可直接记录业务项目版本线、负责人与升级窗口。
- VitePress 导航、总览页与维护者入口已接入新文档，避免孤立页面。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。

## Portal 老组件样式复刻收口（9 组件迁移二阶段）

### 范围

- `packages/portal-engine/src/materials/cms/publicity-education/**`
- `packages/portal-engine/src/materials/cms/mail-list/**`
- `packages/portal-engine/src/materials/cms/dept-upload-files/**`
- `packages/portal-engine/src/materials/base/app-entrance/**`
- `packages/portal-engine/src/materials/base/image-link-list/**`
- `packages/portal-engine/src/materials/cms/image-text-column/index.vue`
- `packages/portal-engine/src/materials/cms/carousel-text-list/index.vue`
- `apps/docs/docs/guide/portal/material-extension.md`

### 结论

- 9 个迁移组件维持新项目配置协议（统一容器 + 公共配置字段）不变。
- 渲染层样式收口为老项目视觉基线：
  - `publicity-education`、`mail-list`、`dept-upload-files`、`app-entrance`、`image-link-list` 已按老样式结构复刻。
- 兼容细节补齐：
  - `image-text-column` 恢复 `showDot` 真实绑定；
  - `carousel-text-list` 轮播图图片源兼容 `coverUrl` / `carouselUrl`。
- `app-entrance` 补齐老样式资源 `icon-add.svg`，并保留新链路跳转配置模型。

### 验证命令

```bash
pnpm -C packages/portal-engine typecheck
pnpm -C packages/portal-engine lint
pnpm -C packages/portal-engine test:run src/materials/useRendererMaterials.test.ts src/registry/materials-registry.aliases.test.ts
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/portal-engine`：`typecheck` 通过。
- `packages/portal-engine`：`lint` 0 warning / 0 error。
- `packages/portal-engine`：2 个测试文件 `5/5` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## admin-lite 同步跟进 admin 最新改动（ObTable 基线）

### 范围

- `apps/admin-lite/src/modules/adminManagement/**`
- `apps/admin-lite/src/modules/LogManagement/login-log/list.vue`
- `apps/admin-lite/src/modules/LogManagement/sys-log/list.vue`
- `apps/admin-lite/src/modules/SystemManagement/dict/list.vue`
- `apps/admin-lite/src/bootstrap/plugins.ts`
- `apps/admin-lite/tests/router/route-policy.unit.test.ts`
- `apps/admin-lite/src/modules/adminManagement/**/**.unit.test.ts`（定向）
- `apps/admin-lite/AGENTS.md`
- `apps/admin-lite/README.md`
- `apps/docs/docs/guide/admin-lite-agent-redlines.md`
- `apps/docs/docs/guide/admin-lite-base-app.md`
- `scripts/check-admin-lite-arch.mjs`

### 结论

- `admin-lite` 已同步 `admin` 最新管理模块实现，并补齐菜单内嵌宿主页能力（`/ext/:slug(.*)*`、`/micro/:slug(.*)*` 对应页面）。
- `admin-lite` 列表基线已从 `ObVxeTable` 收口到 `ObTable`：
  - 插件入口切换为 `@one-base-template/ui/obtable`。
  - 规则、README 与 docs 指南口径一致。
  - 架构门禁文案同步更新为 `ObTable`。
- admin-lite 定向测试断言已对齐最新行为（用户状态、角色权限弹窗、route-policy）。

### 验证命令

```bash
pnpm -C apps/admin-lite typecheck
pnpm -C apps/admin-lite lint
pnpm -C apps/admin-lite lint:arch
pnpm -C apps/admin-lite build
pnpm -C apps/admin-lite test:run
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin-lite`：`typecheck` 通过。
- `apps/admin-lite`：`lint` 0 warning / 0 error。
- `apps/admin-lite`：`lint:arch` 通过。
- `apps/admin-lite`：`build` 通过。
- `apps/admin-lite`：测试 `40 files / 110 tests` 全通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

## SSO 统一配置入口 + ticket serviceUrl + portal /sso 闭环

### 范围

- `apps/admin/src/config/auth-sso.ts`
- `apps/admin/src/config/index.ts`
- `apps/admin/src/config/sso.ts`
- `apps/admin/src/services/auth/auth-remote-service.ts`
- `apps/admin/src/services/auth/auth-scenario-provider.ts`
- `apps/admin/tests/services/auth/auth-scenario-provider.unit.test.ts`
- `apps/admin-lite/src/config/auth-sso.ts`
- `apps/admin-lite/src/config/index.ts`
- `apps/admin-lite/src/config/sso.ts`
- `apps/admin-lite/src/services/auth/auth-remote-service.ts`
- `apps/admin-lite/src/services/auth/auth-scenario-provider.ts`
- `apps/admin-lite/tests/services/auth/auth-scenario-provider.unit.test.ts`
- `packages/core/src/auth/sso-callback-strategy.ts`
- `packages/core/src/auth/sso-callback-strategy.test.ts`
- `apps/portal/src/config/sso.ts`
- `apps/portal/src/bootstrap/index.ts`
- `apps/portal/src/router/constants.ts`
- `apps/portal/src/router/routes.ts`
- `apps/portal/src/pages/sso/SsoCallbackPage.vue`
- `apps/docs/docs/guide/env.md`
- `apps/docs/docs/guide/module-system.md`

### 结论

- admin/admin-lite 的 SSO 策略与 SSO 接口地址已统一收口到 `config/auth-sso.ts`，`config/sso.ts` 仅保留兼容导出。
- ticket 登录链路与后端契约对齐：`serviceUrl` 显式参数优先；缺省时再回退 `redirectUrl` 或当前地址。
- core 的 SSO 参数策略层已透传 `serviceUrlRaw`，应用层不再自行硬编码 ticket 参数来源。
- portal 已补齐 `/sso` 路由与回调页，`bootstrap` 已开启 `sso.enabled=true`（通过 `appSsoOptions`），失败分支统一清理 `tokenKey/idTokenKey`。
- 文档已同步到新入口与新口径（auth-sso 统一入口、portal `/sso` 闭环）。

### 验证命令

```bash
pnpm -C apps/admin test:run -- tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin-lite test:run -- tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin-lite typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin-lite lint
pnpm -C apps/portal lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/admin`：测试通过（`74 files / 177 tests`），lint 无 error（4 条既有 `max-lines` warning）。
- `apps/admin-lite`：测试通过（`40 files / 112 tests`），`typecheck` 通过，lint 0 warning / 0 error。
- `apps/portal`：lint 0 warning / 0 error。
- `apps/docs`：lint 0 warning / 0 error，build 成功。

### 未通过项（并行改动导致）

- `pnpm -C packages/core test:run -- src/auth/sso-callback-strategy.test.ts`
  - 失败点：`packages/core/src/router/module-registry.test.ts`。
- `pnpm -C apps/portal typecheck`
  - 失败点：`packages/portal-engine/src/materials/base/app-entrance/index.vue`。
- `pnpm -C apps/admin typecheck`
  - 失败点：`packages/portal-engine/src/materials/base/app-entrance/index.vue`、
    `packages/portal-engine/src/materials/cms/publicity-education/index.vue`。

## 模块契约收口：manifest.ts 合并到 module.ts（moduleMeta）

### 范围

- `apps/admin/src/modules/**/module.ts`
- `apps/admin-lite/src/modules/**/module.ts`
- `apps/admin/src/modules/**/manifest.ts`（删除）
- `apps/admin-lite/src/modules/**/manifest.ts`（删除）
- `apps/admin/src/router/registry.ts`
- `apps/admin-lite/src/router/registry.ts`
- `packages/core/src/router/module-registry.ts`
- `packages/core/src/router/module-registry.test.ts`
- `packages/core/src/index.ts`
- `scripts/new-module.mjs`
- `scripts/new-app.mjs`
- `apps/admin-lite/AGENTS.md`
- `apps/docs/docs/guide/admin-lite-agent-redlines.md`
- `apps/docs/docs/guide/module-system.md`
- `apps/docs/docs/guide/{architecture,architecture-runtime-deep-dive,for-users,index,agents-scope,zfw-system-sfss-quick-start}.md`
- `apps/docs/docs/public/diagrams/module-entry-tree.svg`

### 结论

- 模块契约已从“`manifest.ts + module.ts` 双文件”收口为“`module.ts（内含 moduleMeta） + routes.ts`”。
- 路由注册链路已统一：`registry` 先从 `module.ts` eager 读取 `moduleMeta`，再按 `enabledModules` 动态导入同路径模块声明。
- core 注册器不再依赖 `manifestPath`，并保持 `optional + enabledByDefault=false` 校验红线。
- 脚手架模板已同步，后续新建模块默认不会再生成 `manifest.ts`。
- 文档与 AGENTS 已同步到新口径，并新增“为什么合并到 module.ts”说明，便于对内解释设计取舍。
- 根 `package.json` 已校验，未发现可直接删除的无效脚本指令（`scripts/*.mjs` 引用文件均存在）。

### 验证命令

```bash
pnpm -C packages/core test:run -- src/router/module-registry.test.ts
pnpm -C packages/core typecheck
pnpm -C apps/admin test:run:file -- tests/router/route-policy.unit.test.ts
pnpm -C apps/admin-lite typecheck
pnpm -C apps/admin-lite test:run:file -- tests/router/route-policy.unit.test.ts
pnpm -C apps/admin-lite lint:arch
pnpm -C apps/docs lint
pnpm -C apps/docs build
pnpm -C apps/admin typecheck
```

### 验证结果

- `packages/core`：模块注册相关测试 `25 files / 120 tests` 通过，`typecheck` 通过。
- `apps/admin`：路由策略定向测试 `1 file / 3 tests` 通过。
- `apps/admin-lite`：`typecheck` 通过；路由策略定向测试 `1 file / 3 tests` 通过；`lint:arch` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 未通过项（并行改动导致）

- `pnpm -C apps/admin typecheck`
  - 失败点：`packages/portal-engine/src/materials/base/app-entrance/index.vue`、`packages/portal-engine/src/materials/cms/publicity-education/index.vue`（类型报错，非本次模块契约改动引入）。

## SSO 收口二次调整（去兼容导出 + adapters 端点注入）

### 范围

- `apps/admin/src/bootstrap/adapter.ts`
- `apps/admin/src/bootstrap/index.ts`
- `apps/admin/src/config/sso.ts`（删除）
- `apps/admin/tests/bootstrap/index.unit.test.ts`
- `apps/admin-lite/src/bootstrap/adapter.ts`
- `apps/admin-lite/src/bootstrap/index.ts`
- `apps/admin-lite/src/config/sso.ts`（删除）
- `apps/admin-lite/tests/bootstrap/index.unit.test.ts`
- `apps/portal/src/config/sso.ts`
- `apps/portal/src/bootstrap/adapter.ts`
- `apps/portal/src/bootstrap/index.ts`
- `packages/adapters/src/basicAdapter.ts`
- `apps/docs/docs/guide/env.md`
- `apps/admin-lite/README.md`

### 结论

- 用户要求“不要兼容导出”已落实：`admin/admin-lite` 的 `config/sso.ts` 已删除，SSO 入口统一为 `config/auth-sso.ts`。
- `packages/adapters` 的 basic ticket SSO 端点已改为注入参数：
  - `createBasicAdapter` 不再内置 hardcode；
  - 由应用启动层显式传入 `ticketSsoEndpoint`。
- `admin`、`admin-lite`、`portal` 三端均已在 bootstrap -> adapter 链路注入 ticket 端点，
  端点来源统一收敛到各应用配置文件。

### 验证命令

```bash
pnpm -C packages/adapters typecheck
pnpm -C packages/adapters lint
pnpm -C apps/admin test:run -- tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin-lite test:run -- tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin-lite typecheck
pnpm -C apps/admin lint
pnpm -C apps/admin-lite lint
pnpm -C apps/portal lint
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `packages/adapters`：typecheck 通过，lint 0 warning / 0 error。
- `apps/admin`：测试通过（`74 files / 177 tests`），lint 无 error（4 条既有 `max-lines` warning）。
- `apps/admin-lite`：测试通过（`40 files / 112 tests`），typecheck 通过，lint 0 warning / 0 error。
- `apps/portal`：lint 0 warning / 0 error。
- `apps/docs`：lint 0 warning / 0 error，build 成功。

### 未通过项（并行改动导致）

- `pnpm -C apps/admin typecheck`：`packages/portal-engine` 类型错误（与本次 SSO 改动无关）。
- `pnpm -C apps/portal typecheck`：`packages/portal-engine` 类型错误（与本次 SSO 改动无关）。

## 模块入口命名收口：module.ts -> index.ts

### 范围

- `apps/admin/src/modules/**/{module.ts -> index.ts}`
- `apps/admin-lite/src/modules/**/{module.ts -> index.ts}`
- `apps/admin/src/router/registry.ts`
- `apps/admin-lite/src/router/registry.ts`
- `packages/core/src/router/module-registry.ts`
- `packages/core/src/router/module-registry.test.ts`
- `scripts/new-module.mjs`
- `scripts/new-app.mjs`
- `apps/admin-lite/AGENTS.md`
- `apps/admin/README.md`
- `apps/docs/docs/guide/{module-system,menu-route-spec,architecture,architecture-runtime-deep-dive,development,naming-whitelist,admin-lite-agent-redlines,for-users,zfw-system-sfss-quick-start}.md`
- `apps/docs/public/cli-naming-whitelist.json`
- `apps/docs/docs/public/diagrams/module-entry-tree.svg`

### 结论

- 模块入口文件命名已统一为 `index.ts`，不再保留 `module.ts` 命名。
- admin/admin-lite/core 的模块扫描与声明校验链路已统一切到 `**/index.ts`。
- 脚手架输出与文档规则已同步，新增模块默认直接使用 `index.ts（含 moduleMeta） + routes.ts`。

### 验证命令

```bash
pnpm -C packages/core test:run -- src/router/module-registry.test.ts
pnpm -C packages/core typecheck
pnpm -C apps/admin test:run:file -- tests/router/route-policy.unit.test.ts
pnpm -C apps/admin lint:arch
pnpm -C apps/admin-lite test:run:file -- tests/router/route-policy.unit.test.ts
pnpm -C apps/admin-lite lint:arch
pnpm -C apps/admin-lite typecheck
pnpm -C apps/docs lint
pnpm -C apps/docs build
node --test scripts/__tests__/new-app.test.mjs
pnpm new:module codex-temp-demo --dry-run
```

### 验证结果

- `packages/core`：模块注册相关测试 `25 files / 120 tests` 通过，`typecheck` 通过。
- `apps/admin`：路由策略定向测试通过，`lint:arch` 通过。
- `apps/admin-lite`：路由策略定向测试通过，`lint:arch` 通过，`typecheck` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。
- 脚手架验证：`new:module --dry-run` 已输出 `index.ts` 入口。

## portal 登录域收口（提示栈统一 + 登录场景统一 + SSO 端点收口 + meta.access 迁移）

### 范围

- `apps/portal/src/bootstrap/index.ts`
- `apps/portal/src/bootstrap/http.ts`
- `apps/portal/src/pages/login/LoginPage.vue`
- `apps/portal/src/pages/sso/SsoCallbackPage.vue`
- `apps/portal/src/services/auth/auth-remote-service.ts`
- `apps/portal/src/config/sso.ts`
- `apps/portal/src/router/routes.ts`
- `apps/portal/src/router/constants.ts`
- `apps/docs/docs/guide/env.md`
- `apps/docs/docs/guide/development.md`
- `apps/docs/docs/guide/module-system.md`

### 结论

- portal 反馈能力已和 admin 口径统一：登录页、SSO 页、HTTP `onBizError` 均使用 `@one-base-template/ui` 的 `message`。
- portal 登录页已接入 core `buildLoginScenario`，basic/default 分支判定不再散落在页面里手写。
- portal 登录相关远端接口地址已收口到 `config/sso.ts`（登录页配置、前台配置、ticket SSO）。
- portal 路由访问语义已从 `meta.public` 迁移到 `meta.access`：
  - `open`：`/`、`/login`、`/sso`、`/403`、`/404`
  - `auth`：`/portal/index`、`/portal/preview`
- 文档已同步更新，仓库内规则描述与代码行为一致。

### 验证命令

```bash
pnpm -C apps/portal typecheck
pnpm -C apps/portal lint
pnpm -C apps/portal build
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/portal`：`lint` 0 warning / 0 error；`build` 通过。
- `apps/docs`：`lint` 0 warning / 0 error；`build` 成功。

### 未通过项（存量问题）

- `pnpm -C apps/portal typecheck`
  - 失败点：`packages/portal-engine/src/materials/base/app-entrance/index.vue`。
  - 错误：`Type 'unknown' is not assignable to type 'string | undefined'`（及 `PortalLinkOpenType | undefined`）。
  - 说明：该问题在本轮改动前已存在，本次未改动 `packages/portal-engine` 对应文件。

## portal 登录域收口（补充验证：portal typecheck 阻塞已清除）

### 增量改动

- `packages/portal-engine/src/materials/base/app-entrance/index.vue`
  - 在 `resolveRowLinkConfig` 对 `linkPath/linkParamKey/linkValueKey/openType` 的 `unknown` 值进行类型收敛，避免直接赋给 `Partial<PortalLinkConfig>`。

### 验证命令

```bash
pnpm -C apps/portal typecheck
pnpm -C apps/portal lint
pnpm -C apps/portal build
```

### 验证结果

- `apps/portal typecheck`：通过。
- `apps/portal lint`：通过（0 warning / 0 error）。
- `apps/portal build`：通过。

### 结论

- 本轮 portal 登录域 P0 收口任务，当前验证口径下已全部通过。

## 登录域收口（补充验证：admin/admin-lite/core 联动）

### 增量改动

- `packages/portal-engine/src/materials/cms/publicity-education/index.vue`
  - 新增 `resolveCarouselArrow`，将轮播 `arrow` 显式收敛为 `"always" | "hover" | "never"`，修复模板绑定类型不匹配。

### 验证命令

```bash
pnpm -C apps/admin test:run:file -- tests/bootstrap/index.unit.test.ts
pnpm -C apps/admin test:run:file -- tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin typecheck
pnpm -C apps/admin-lite test:run:file -- tests/bootstrap/index.unit.test.ts
pnpm -C apps/admin-lite test:run:file -- tests/services/auth/auth-scenario-provider.unit.test.ts
pnpm -C apps/admin-lite typecheck
pnpm -C packages/core test:run -- src/auth/sso-callback-strategy.test.ts
pnpm -C apps/portal typecheck
```

### 验证结果

- admin：2 个登录相关定向测试通过；`typecheck` 通过。
- admin-lite：2 个登录相关定向测试通过；`typecheck` 通过。
- core：`sso-callback-strategy` 测试通过（25 files / 120 tests）。
- portal：`typecheck` 通过。

### 结论

- 这轮“admin 与 portal 登录体系底层对齐（不含菜单）”相关 P0 改动，验证口径下已闭环通过。

## 架构治理补强：命名门禁 + admin-lite 体积门禁 + new-module 测试

### 范围

- `scripts/check-naming.mjs`
- `apps/admin/src/router/route-policy.ts`
- `apps/admin-lite/src/router/route-policy.ts`
- `apps/admin/src/modules/DocumentFormManagement/services/template-service.ts`
- `apps/admin/src/modules/PortalManagement/materials/extensions/index.ts`
- `scripts/check-admin-lite-build-size.mjs`
- `package.json`
- `apps/docs/docs/guide/development.md`
- `apps/admin-lite/AGENTS.md`
- `apps/admin-lite/README.md`
- `scripts/new-module.mjs`
- `scripts/__tests__/new-module.test.mjs`

### 结论

- 命名治理已与模块契约对齐：`check-naming` 不再误扫 `module.ts`，改为校验 `index.ts` 模块入口。
- 既有命名违规已收敛：`check:naming` 当前通过。
- `admin-lite` 已新增与 `admin` 同级的构建体积预算门禁，并接入根 `verify`，避免基座项目体积回归缺口。
- `new-module` 已支持可导入测试（`parseArgs`/`scaffoldModule`），并补齐 `node:test` 级别自动化断言。

### 验证命令

```bash
pnpm check:naming
pnpm check:admin:bundle
pnpm check:admin-lite:bundle
node --test scripts/__tests__/new-module.test.mjs scripts/__tests__/new-app.test.mjs
pnpm -C apps/admin test:run:file -- tests/router/route-policy.unit.test.ts
pnpm -C apps/admin-lite test:run:file -- tests/router/route-policy.unit.test.ts
pnpm new:module codex-temp-module --dry-run
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `check:naming` 通过（schema 校验通过，命名扫描无违规）。
- `check:admin:bundle` 通过（startup js count `7/22`，startup js gzip `141.1KiB/820KiB`）。
- `check:admin-lite:bundle` 通过（startup js count `7/22`，startup js gzip `140.8KiB/820KiB`）。
- `scripts` 测试 `5/5` 通过（含新增 `new-module` 测试）。
- admin/admin-lite 路由策略定向单测均通过（各 `1 file / 3 tests`）。
- `new:module --dry-run` 输出文件列表为 `index.ts + routes.ts + api + services + pages`。
- `apps/docs`：`lint` 0 warning / 0 error，`build` 成功。

## docs：补充 1 分钟版路由 meta 速查卡

### 范围

- `apps/docs/docs/guide/menu-route-spec.md`

### 结论

- 已在文档页顶部新增 `TL;DR：1分钟路由 Meta 速查卡`，收敛到“先看就能用”的内容：
  - `meta.access` 三态语义与登录/菜单权限对照；
  - helper 写法模板；
  - 30 秒自检清单（`open/hiddenTab`、`keepAlive/name`、`activePath` 约束）。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。

## docs 收口：开发阶段去历史迁移展示 + 表格口径统一

### 范围

- `apps/docs/docs/.vitepress/config.ts`
- `apps/docs/docs/guide/index.md`
- `apps/docs/docs/guide/for-users.md`
- `apps/docs/docs/guide/quick-start.md`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/development.md`
- `apps/docs/docs/guide/agents-scope.md`
- `apps/docs/docs/guide/module-system.md`
- `apps/docs/docs/guide/portal/admin-designer.md`

### 结论

- 开发阶段入口已去除历史迁移展示链路，docs 导航与入口页聚焦当前开发主线。
- 环境要求已与仓库真实约束对齐（Node `>=20.19.0`，pnpm `>=10.32.1`）。
- 表格说明主线已统一为 `ObTable`，减少 `ObTable/ObVxeTable` 并列表达造成的认知负担。
- 并行开发产生的 `apps/admin*/src/types/*.d.ts` 与未跟踪文件 `=` 已按用户确认忽略，不纳入本次提交范围。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。

## docs：按水平入口收口（P2/P4/P6 主入口）

### 范围

- `apps/docs/docs/guide/index.md`
- `apps/docs/docs/guide/for-users.md`
- `apps/docs/docs/guide/for-maintainers.md`
- `apps/docs/docs/.vitepress/config.ts`

### 结论

- 文档总览页已新增“按水平进入（推荐主入口）”，将 P2/P4/P6 作为首选阅读路径。
- 角色入口继续保留，但定位为“辅助分流”，降低“先选角色还是先选任务”的认知负担。
- `for-users/for-maintainers` 顶部均已提示优先走分层入口，避免入口分叉造成上手犹豫。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。

## platform-config 列表化（代码注释 + docs 逐项解释）

### 范围

- `apps/admin/src/config/platform-config.ts`
- `apps/admin-lite/src/config/platform-config.ts`
- `apps/docs/docs/guide/env.md`

### 结论

- `admin` 与 `admin-lite` 的静态平台配置入口已补齐逐项清单注释，配置项语义可直接在代码中读取。
- docs 侧“代码静态平台配置”已由“核心字段摘要”改为“完整列表 + 每项说明”，减少接手同学对字段含义的猜测成本。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。

## docs 可读性优化（主线中文化 + module-system 最终版）

### 范围

- `apps/docs/docs/guide/levels/index.md`
- `apps/docs/docs/guide/levels/p2.md`
- `apps/docs/docs/guide/levels/p4.md`
- `apps/docs/docs/guide/levels/p6.md`
- `apps/docs/docs/guide/module-system.md`

### 结论

- P2/P4/P6 阅读入口中的“必读主线”已改为中文名称可点击，减少路径文本噪音。
- `module-system` 页面已收口为“最终版”，去除历史过程说明，只保留当前有效契约与执行链路。
- 目录展示统一采用 `├── / └── / │` 树形风格，并显著减少反引号密度与长段落阅读负担。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。

## docs 收口二轮（导航分类 + 页面减负）

### 范围

- `apps/docs/docs/.vitepress/config.ts`
- `apps/docs/docs/guide/index.md`
- `apps/docs/docs/guide/for-users.md`
- `apps/docs/docs/guide/development.md`
- `apps/docs/docs/guide/layout-menu.md`
- `apps/docs/docs/guide/crud-module-best-practice.md`
- `apps/docs/docs/guide/built-in-components.md`
- `apps/docs/docs/guide/portal/admin-designer.md`
- `apps/admin/src/config/layout.ts`

### 结论

- 开发实践导航已按主题分组，CRUD 入口收口，表格链接纠正，按钮样式入口已移除。
- 扩展能力导航已按“公文表单 / 门户设计器 / basic Adapter”三类分组，侧边栏按分类显示。
- `development/layout-menu/crud-module-best-practice` 三页已显著精简，信息密度下降但可执行性提升。
- 已新增“内置组件（Ob 系列）”页面，覆盖核心 `Ob*` 组件与最小接入示例。
- `layout.ts` 已补齐配置注释，布局项语义可直接从代码理解。

### 验证命令

```bash
pnpm -C apps/docs lint
pnpm -C apps/docs build
pnpm -C apps/admin typecheck
```

### 验证结果

- `apps/docs`：`lint` 0 warning / 0 error。
- `apps/docs`：`build` 成功。
- `apps/admin`：`typecheck` 通过。
