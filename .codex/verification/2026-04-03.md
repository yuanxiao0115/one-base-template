# 2026-04-03 验证记录（system-sfss 迁移收口：移除 I\* 二次封装）

## 结论摘要

- 已完成：`apps/zfw-system-sfss` 的 `I*` 本地转发壳全部移除，业务页面切换为 `ObTableBox + ObTable` 及原生 `el-*`，不再走二次封装映射链路。
- 已通过：`pnpm -C apps/zfw-system-sfss lint:arch`。
- 已通过：`pnpm -C apps/docs lint && pnpm -C apps/docs build`（文档站与规则说明同步可用）。
- 未通过：`typecheck` 与 `lint`（legacy 业务代码历史类型债务仍需后续专项收口）。

## 关键验证命令

```bash
pnpm -C apps/zfw-system-sfss lint:arch
pnpm -C apps/zfw-system-sfss typecheck
pnpm -C apps/zfw-system-sfss lint
rg -n "<I[A-Z]|</I[A-Z]|\bI(Button|Form|FormItem|Row|Col|DatePicker|Input|Drawer|Popconfirm)\b" apps/zfw-system-sfss/src -g '*.vue'
rg -n "OneTableBar|PureTable" apps/zfw-system-sfss/src -g '*.*'
```

## 风险与后续

- 当前最大风险不是架构违规，而是 legacy 页面类型严格性：
  - `defineModel()` 默认 `unknown` 导致表单字段访问报错；
  - 历史显式 `any` 与 `Number` 包装类型触发 lint 规则；
  - 部分路由数组的 `RouteRecordRaw` union 推断需显式收口。
- 建议下一步执行“类型债务批处理”专项，分批修复 `system-sfss` 关键业务页（优先 `sunshine-petition` 与 `litigation-related`）。

---

## 2026-04-03（补充）目录层级修正验证：对齐 `adminManagement` 同层结构

### 结论

- `apps/zfw-system-sfss/src/modules/system-sfss` 已去掉额外的 `views/System-sfss` 目录层级。
- 6 个子模块已平铺在 `system-sfss/*` 下，结构与 `adminManagement/*` 的“模块根下一层即子模块”一致。
- 路由聚合已切到同层引入路径，无旧层级残留引用。

### 证据

- 目录扫描：
  - `find apps/zfw-system-sfss/src/modules/system-sfss -maxdepth 2 -type d`
- 路由引入：
  - `apps/zfw-system-sfss/src/modules/system-sfss/routes.ts`（`./sunshine-petition/router` 等同层路径）
- 验证命令：
  - `pnpm -C apps/zfw-system-sfss lint:arch`
  - `pnpm -C apps/zfw-system-sfss test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts`

### 验证结果

- `lint:arch`：通过
- `vitest`：`4 files / 12 tests` 通过

---

## 2026-04-03（补充）system-sfss 根级目录瘦身与自动路由注入

### 结论

- `system-sfss` 根级 `pages/api/services` 过渡目录已移除，仅保留 `index.ts + routes.ts + 6 子模块`。
- `/system-sfss/index` 改为重定向，不再依赖占位页面组件。
- `routes.ts` 已切换为自动收集子模块路由文件（`./*/router/index.ts` + `./*/router.ts`），不再手工维护 import 列表。

### 核验证据

- 自动注入实现：
  - `apps/zfw-system-sfss/src/modules/system-sfss/routes.ts`
- 冗余文件删除：
  - `apps/zfw-system-sfss/src/modules/system-sfss/pages/SystemSfssIndexPage.vue`
  - `apps/zfw-system-sfss/src/modules/system-sfss/pages/SystemSfssLegacyPage.vue`
  - `apps/zfw-system-sfss/src/modules/system-sfss/pages/SystemSfssTodoPage.vue`
  - `apps/zfw-system-sfss/src/modules/system-sfss/api/client.ts`
  - `apps/zfw-system-sfss/src/modules/system-sfss/api/endpoints.ts`

### 验证命令

- `pnpm -C apps/zfw-system-sfss lint:arch`
- `pnpm -C apps/zfw-system-sfss test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts`
- `pnpm -C apps/zfw-system-sfss build`
- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

### 验证结果

- 架构边界检查：通过
- 路由相关测试：`4 files / 12 tests` 通过
- zfw app 构建：通过
- docs lint/build：通过

---

## 2026-04-03（补充）自动路由注入推广到 admin / admin-lite

### 结论

- 已把“子路由自动注入”抽象为 core 公共能力：`collectGlobRouteModules`。
- `admin` 与 `admin-lite` 的 `LogManagement` 均已改为 `routes/*.ts` 自动收集，后续新增日志子路由不再需要手工维护 import 清单。
- `zfw-system-sfss/system-sfss` 已切到同一 helper，三端路由聚合行为统一。

### 核验证据

- 公共能力：
  - `packages/core/src/router/route-utils.ts`
  - `packages/core/src/router/route-utils.test.ts`
- admin：
  - `apps/admin/src/modules/LogManagement/routes.ts`
  - `apps/admin/src/modules/LogManagement/routes/login-log.ts`
  - `apps/admin/src/modules/LogManagement/routes/sys-log.ts`
- admin-lite：
  - `apps/admin-lite/src/modules/LogManagement/routes.ts`
  - `apps/admin-lite/src/modules/LogManagement/routes/login-log.ts`
  - `apps/admin-lite/src/modules/LogManagement/routes/sys-log.ts`
- zfw：
  - `apps/zfw-system-sfss/src/modules/system-sfss/routes.ts`

### 验证结果

- `packages/core`：`route-utils` 定向单测通过，`typecheck` 通过。
- `apps/admin`：路由相关测试 `4 files / 11 tests` 通过；`lint:arch`/`typecheck` 通过。
- `apps/admin-lite`：路由相关测试 `4 files / 11 tests` 通过；`lint:arch`/`typecheck` 通过。
- `apps/zfw-system-sfss`：`lint:arch` 与路由定向测试 `4 files / 12 tests` 通过。
- `apps/zfw-system-sfss typecheck`：未通过（既有 legacy 类型债务，非本次路由注入改动引入）。
- `apps/docs`：`lint` 与 `build` 通过。

---

## 2026-04-03（补充）zfw-system-sfss 接口对接完成：三链路代理可联调

### 结论

- `apps/zfw-system-sfss` 已完成本地联调链路补齐：`/cmict`、`/zfw`、`/zb` 三类接口路径均有对应代理策略。
- 登录/菜单接口继续走 `/cmict/**`，`system-sfss` 业务接口的 `/zfw/**`、指标导出类 `/zb/**` 已具备联调入口。
- 环境说明已同步到 app README 与 docs 快速手册，减少同事接手心智负担。

### 核验证据

- 代理配置：
  - `apps/zfw-system-sfss/vite.config.ts`
- 环境样例：
  - `apps/zfw-system-sfss/.env.example`
- 说明文档：
  - `apps/zfw-system-sfss/README.md`
  - `apps/docs/docs/guide/zfw-system-sfss-quick-start.md`

### 验证结果

- `pnpm -C apps/zfw-system-sfss lint:arch`：通过
- `pnpm -C apps/zfw-system-sfss test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts`：通过（`4 files / 12 tests`）
- `pnpm -C apps/zfw-system-sfss build`：通过
- `pnpm -C apps/docs lint && pnpm -C apps/docs build`：通过

---

## 2026-04-03（补充）zfw/admin-lite：删除中转层 + 单系统菜单收口

### 结论

- `apps/zfw-system-sfss` 已完成 `utils/http`、`utils/message`、`utils/legacy-compat`、`utils/index` 中转层删除，业务代码改为直连官方能力：
  - `@one-base-template/core` 的 `obHttp()`
  - `@one-base-template/ui` 的 `message`
  - `@one-base-template/utils` 的 `cloneDeep/formatTime`
- `admin-lite` 与 `zfw-system-sfss` 已同步接入 `singleSystem` 推导与适配器收口：`preset=*-single` 时即便后端返回多系统，也只注入 `defaultSystemCode` 对应系统。
- 文档已同步更新单系统行为说明与维护口径。

### 核验证据

- 中转层删除：
  - `apps/zfw-system-sfss/src/utils/http/index.ts`（已删除）
  - `apps/zfw-system-sfss/src/utils/message.ts`（已删除）
  - `apps/zfw-system-sfss/src/utils/legacy-compat.ts`（已删除）
  - `apps/zfw-system-sfss/src/utils/index.ts`（已删除）
- 单系统收口代码：
  - `apps/zfw-system-sfss/src/config/env.ts`
  - `apps/zfw-system-sfss/src/bootstrap/adapter.ts`
  - `apps/zfw-system-sfss/src/bootstrap/index.ts`
  - `apps/admin-lite/src/config/env.ts`
  - `apps/admin-lite/src/bootstrap/adapter.ts`
  - `apps/admin-lite/src/bootstrap/index.ts`
- 工具导出对齐：
  - `packages/utils/src/object/index.ts`
  - `packages/utils/src/index.ts`

### 验证结果

- `pnpm -C apps/zfw-system-sfss lint:arch`：通过
- `pnpm -C apps/zfw-system-sfss build`：通过
- `pnpm -C apps/admin-lite typecheck`：通过
- `pnpm -C apps/admin-lite lint:arch`：通过
- `pnpm -C apps/admin-lite build`：通过
- `pnpm -C apps/docs lint`：通过
- `pnpm -C apps/docs build`：通过
- `pnpm -C apps/zfw-system-sfss typecheck`：失败（既有 legacy 类型债务，非本次中转层收口新增）

---

## 2026-04-03（补充）platformConfig 模块化维护 + 切账号权限缓存防串号

### 结论

- `admin/admin-lite/zfw-system-sfss` 的 `platform-config.ts` 已按模块拆分配置对象，并通过展开合并生成最终配置，阅读与维护成本下降。
- remote 菜单模式新增安全约束：`remoteSynced=false` 时必须先同步菜单，失败时返回 `403`，不再按缓存菜单放行。
- token/mixed 模式首次守卫新增强校验：`ensureAuthed()` 在初始化阶段会先 `fetchMe()`，防止切账号后沿用旧用户缓存。

### 关键文件

- `apps/admin/src/config/platform-config.ts`
- `apps/admin-lite/src/config/platform-config.ts`
- `apps/zfw-system-sfss/src/config/platform-config.ts`
- `packages/core/src/router/guards.ts`
- `packages/core/src/stores/auth.ts`

### 验证命令

- `pnpm -C packages/core test:run -- src/stores/auth.test.ts src/router/guards.test.ts src/config/platform-config.test.ts`
- `pnpm -C packages/core lint`
- `pnpm -C apps/admin test:run:file -- tests/config/platform-config.unit.test.ts`
- `pnpm -C apps/admin-lite test:run:file -- tests/config/platform-config.unit.test.ts`
- `pnpm -C apps/zfw-system-sfss test:run:file -- tests/config/platform-config.unit.test.ts`

### 验证结果

- `packages/core`：`27 files / 132 tests` 全通过。
- `packages/core lint`：0 warning / 0 error。
- 三应用 `platform-config` 定向单测均通过（各 `1 file / 2 tests`）。

---

## 2026-04-03（补充）system-sfss 列表页结构统一（ObPageContainer + ObTableBox + ObTable）

### 结论

- `system-sfss` 剩余列表页已统一到 `admin` 同构容器结构：`ObPageContainer padding="0" overflow="hidden" -> ObTableBox -> ObTable`。
- `petition-topic-contrast` 已去除 `height="calc(100vh - 280px)"` 硬编码，改为 `ObTable adaptive`，降低滚动条/空白区异常风险。
- `collaboration-statistics` 已将 `Search + ObTable` 收口到 `ObTableBox` 内容区，页面高度链统一。

### 关键文件

- `apps/zfw-system-sfss/src/modules/system-sfss/sunshine-petition/components/visit-list.vue`
- `apps/zfw-system-sfss/src/modules/system-sfss/litigation-related/prosecution-related/index.vue`
- `apps/zfw-system-sfss/src/modules/system-sfss/litigation-related/court-related/index.vue`
- `apps/zfw-system-sfss/src/modules/system-sfss/litigation-related/police-related/index.vue`
- `apps/zfw-system-sfss/src/modules/system-sfss/petition-supervision/pages/dispute-type-contrast/index.vue`
- `apps/zfw-system-sfss/src/modules/system-sfss/petition-supervision/pages/petition-topic-contrast/index.vue`
- `apps/zfw-system-sfss/src/modules/system-sfss/petition-supervision/pages/collaboration-statistics/index.vue`

### 验证结果

- `pnpm -C apps/zfw-system-sfss lint:arch`：通过。
- `pnpm -C apps/zfw-system-sfss typecheck`：失败（既有 legacy 类型债务，非本次布局收口新增）。
- `pnpm -C apps/zfw-system-sfss lint <7 files>`：失败（既有 `no-explicit-any` 存量问题）。
- `@vue/compiler-sfc` 定向解析 7 个改动文件：全部通过（无模板/脚本语法错误）。

---

## 2026-04-03（补充）迁移策略文档增强（Ob 规范 + CRUD 收口）

### 结论

- `monorepo-web-migration-pitfalls` 已升级为可执行迁移策略：
  - 覆盖 CRUD 四件套：`ObPageContainer + ObTableBox + ObTable + ObActionButtons`
  - 覆盖 CRUD 弹层容器：`ObCrudContainer`
  - 覆盖反例到正例的收口路径与执行检查命令。
- 文档中新增“在 admin-lite 内置可开关 CRUD Demo 模块”的后续建议，用于加速后续迁移复制。

### 关键文件

- `apps/docs/docs/guide/monorepo-web-migration-pitfalls.md`

### 验证结果

- `pnpm -C apps/docs lint`：通过（0 warning / 0 error）。
- `pnpm -C apps/docs build`：通过。

---

## 2026-04-03（补充）admin-lite 可开关 starter-crud 示例模块

### 结论

- `admin-lite` 已落地 `starter-crud` 示例模块，默认关闭，不影响现有四模块基线。
- 开启方式已收口到 `apps/admin-lite/src/config/platform-config.ts` 的 `enableStarterCrudDemoModule`，通过模块白名单追加控制。
- 文档已同步到 `admin-lite` 基座页，并替换旧 `preset=remote-single/static-single` 口径为 `systemConfig.mode === 'single'`。

### 关键文件

- `apps/admin-lite/src/modules/starter-crud/index.ts`
- `apps/admin-lite/src/modules/starter-crud/routes.ts`
- `apps/admin-lite/src/modules/starter-crud/list.vue`
- `apps/admin-lite/src/modules/starter-crud/composables/useStarterCrudPageState.ts`
- `apps/admin-lite/src/config/platform-config.ts`
- `apps/admin-lite/src/bootstrap/index.ts`
- `apps/docs/docs/guide/admin-lite-base-app.md`

### 验证命令

- `pnpm -C apps/admin-lite lint:arch`
- `pnpm -C apps/admin-lite typecheck`
- `pnpm -C apps/admin-lite test:run:file -- tests/config/platform-config.unit.test.ts`
- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

### 验证结果

- `apps/admin-lite lint:arch`：通过。
- `apps/admin-lite typecheck`：通过（含修复旧字段 `singleSystem` 传参与类型导入问题后回归通过）。
- `apps/admin-lite platform-config` 定向测试：`1 file / 2 tests` 通过。
- `apps/docs lint/build`：通过。

---

## 2026-04-03（补充）technical-doc-collaboration Skill 交付验证

### 结论

- 新增 Skill `technical-doc-collaboration` 已完成结构化交付，包含 `SKILL.md`、`agents/openai.yaml`、`references/*`、`assets/*`。
- Skill 规则通过 `quick_validate.py` 校验，满足基础格式与命名规范。
- Skill 内容覆盖“受众优先、问题框架、最小可运行示例、全量端点清单、协作评审、Mermaid 流程图”核心能力。

### 关键文件

- `.codex/skills/technical-doc-collaboration/SKILL.md`
- `.codex/skills/technical-doc-collaboration/agents/openai.yaml`
- `.codex/skills/technical-doc-collaboration/references/audience-first-checklist.md`
- `.codex/skills/technical-doc-collaboration/references/collaboration-playbook.md`
- `.codex/skills/technical-doc-collaboration/references/source-notes.md`
- `.codex/skills/technical-doc-collaboration/assets/tech-doc-collab-template.md`

### 验证结果

- `quick_validate.py`：通过（`Skill is valid!`）。
- `generate_openai_yaml.py`：通过（`agents/openai.yaml` 已生成）。

---

## 2026-04-03（补充）docs 技术文档分析与改造（四技能顺序）

### 结论

- 已按顺序完成：`brainstorming -> technical-doc-collaboration -> write-markdown-tech-docs -> vitepress-doc-beauty`。
- 已新增分析方案与方法论页面，并重写关键入口文档，统一“TL;DR + 适用范围 + 步骤 + 验收 + FAQ”骨架。
- 导航、侧边栏、总览页与首页入口已同步，新增页面不是孤立文档。

### 关键文件

- `docs/plans/2026-04-03-docs-tech-doc-refactor-plan.md`
- `apps/docs/docs/guide/tech-doc-collaboration.md`
- `apps/docs/docs/guide/quick-start.md`
- `apps/docs/docs/guide/architecture.md`
- `apps/docs/docs/guide/development.md`
- `apps/docs/docs/guide/index.md`
- `apps/docs/docs/index.md`
- `apps/docs/docs/.vitepress/config.ts`

### 验证命令

- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

### 验证结果

- `apps/docs lint`：通过（0 warning / 0 error）。
- `apps/docs build`：通过（VitePress 构建成功）。

---

## 2026-04-03（补充）docs 第二批结构化改造（env/theme/adapter/utils）

### 结论

- 已完成第二批 docs 页面结构化改造，覆盖 `env`、`theme-system`、`adapter-basic`、`utils-api` 四页。
- 四页均补齐“TL;DR、适用范围、最小可运行路径、验证、FAQ”，并与源码路径对齐。
- 文档改造后 `apps/docs` lint/build 持续通过。

### 关键文件

- `apps/docs/docs/guide/env.md`
- `apps/docs/docs/guide/adapter-basic.md`
- `apps/docs/docs/guide/utils-api.md`
- `apps/docs/docs/guide/theme-system.md`

### 验证命令

- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

### 验证结果

- `apps/docs lint`：通过（0 warning / 0 error）。
- `apps/docs build`：通过（VitePress 构建成功）。

---

## 2026-04-03（补充）docs 第三批结构化改造（角色入口/图标/命名/按钮）

### 结论

- 已按“完整计划 -> 顺序执行”完成第三批文档改造。
- 覆盖角色入口页（for-users/for-maintainers）、专项能力页（iconfont/naming-whitelist）和样式规范页（button-styles）。
- 目标页面已补齐执行路径与验收口径，`apps/docs` lint/build 持续通过。

### 关键文件

- `docs/plans/2026-04-03-docs-tech-doc-refactor-batch3-plan.md`
- `apps/docs/docs/guide/for-users.md`
- `apps/docs/docs/guide/for-maintainers.md`
- `apps/docs/docs/guide/iconfont.md`
- `apps/docs/docs/guide/naming-whitelist.md`
- `apps/docs/docs/guide/button-styles.md`

### 验证命令

- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

### 验证结果

- `apps/docs lint`：通过（0 warning / 0 error）。
- `apps/docs build`：通过（VitePress 构建成功）。
