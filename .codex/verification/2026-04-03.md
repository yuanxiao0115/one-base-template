# 2026-04-03 验证记录（system-sfss 迁移收口：移除 I\* 二次封装）

## 结论摘要

- 已完成：`apps/zfw-system-sfss` 的 `I*` 本地转发壳全部移除，业务页面切换为 `ObTableBox + ObTable` 及原生 `el-*`，不再走二次封装映射链路。
- 已通过：`pnpm -C apps/zfw-system-sfss lint:arch`。
- 已通过：`pnpm -C apps/docs lint && pnpm -C apps/docs build`（文档站与规则说明同步可用）。
- 未通过：`typecheck` 与 `lint`（legacy 业务代码历史类型债务仍需后续专项收口）。

## 关键验证命令

```bash
pnpm -C apps/zfw-system-sfss lint:arch
pnpm -C apps/zfw-system-sfss typecheck
pnpm -C apps/zfw-system-sfss lint
rg -n "<I[A-Z]|</I[A-Z]|\bI(Button|Form|FormItem|Row|Col|DatePicker|Input|Drawer|Popconfirm)\b" apps/zfw-system-sfss/src -g '*.vue'
rg -n "OneTableBar|PureTable" apps/zfw-system-sfss/src -g '*.*'
```

## 风险与后续

- 当前最大风险不是架构违规，而是 legacy 页面类型严格性：
  - `defineModel()` 默认 `unknown` 导致表单字段访问报错；
  - 历史显式 `any` 与 `Number` 包装类型触发 lint 规则；
  - 部分路由数组的 `RouteRecordRaw` union 推断需显式收口。
- 建议下一步执行“类型债务批处理”专项，分批修复 `system-sfss` 关键业务页（优先 `sunshine-petition` 与 `litigation-related`）。

---

## 2026-04-03（补充）目录层级修正验证：对齐 `adminManagement` 同层结构

### 结论

- `apps/zfw-system-sfss/src/modules/system-sfss` 已去掉额外的 `views/System-sfss` 目录层级。
- 6 个子模块已平铺在 `system-sfss/*` 下，结构与 `adminManagement/*` 的“模块根下一层即子模块”一致。
- 路由聚合已切到同层引入路径，无旧层级残留引用。

### 证据

- 目录扫描：
  - `find apps/zfw-system-sfss/src/modules/system-sfss -maxdepth 2 -type d`
- 路由引入：
  - `apps/zfw-system-sfss/src/modules/system-sfss/routes.ts`（`./sunshine-petition/router` 等同层路径）
- 验证命令：
  - `pnpm -C apps/zfw-system-sfss lint:arch`
  - `pnpm -C apps/zfw-system-sfss test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts`

### 验证结果

- `lint:arch`：通过
- `vitest`：`4 files / 12 tests` 通过

---

## 2026-04-03（补充）system-sfss 根级目录瘦身与自动路由注入

### 结论

- `system-sfss` 根级 `pages/api/services` 过渡目录已移除，仅保留 `index.ts + routes.ts + 6 子模块`。
- `/system-sfss/index` 改为重定向，不再依赖占位页面组件。
- `routes.ts` 已切换为自动收集子模块路由文件（`./*/router/index.ts` + `./*/router.ts`），不再手工维护 import 列表。

### 核验证据

- 自动注入实现：
  - `apps/zfw-system-sfss/src/modules/system-sfss/routes.ts`
- 冗余文件删除：
  - `apps/zfw-system-sfss/src/modules/system-sfss/pages/SystemSfssIndexPage.vue`
  - `apps/zfw-system-sfss/src/modules/system-sfss/pages/SystemSfssLegacyPage.vue`
  - `apps/zfw-system-sfss/src/modules/system-sfss/pages/SystemSfssTodoPage.vue`
  - `apps/zfw-system-sfss/src/modules/system-sfss/api/client.ts`
  - `apps/zfw-system-sfss/src/modules/system-sfss/api/endpoints.ts`

### 验证命令

- `pnpm -C apps/zfw-system-sfss lint:arch`
- `pnpm -C apps/zfw-system-sfss test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts`
- `pnpm -C apps/zfw-system-sfss build`
- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

### 验证结果

- 架构边界检查：通过
- 路由相关测试：`4 files / 12 tests` 通过
- zfw app 构建：通过
- docs lint/build：通过

---

## 2026-04-03（补充）自动路由注入推广到 admin / admin-lite

### 结论

- 已把“子路由自动注入”抽象为 core 公共能力：`collectGlobRouteModules`。
- `admin` 与 `admin-lite` 的 `LogManagement` 均已改为 `routes/*.ts` 自动收集，后续新增日志子路由不再需要手工维护 import 清单。
- `zfw-system-sfss/system-sfss` 已切到同一 helper，三端路由聚合行为统一。

### 核验证据

- 公共能力：
  - `packages/core/src/router/route-utils.ts`
  - `packages/core/src/router/route-utils.test.ts`
- admin：
  - `apps/admin/src/modules/LogManagement/routes.ts`
  - `apps/admin/src/modules/LogManagement/routes/login-log.ts`
  - `apps/admin/src/modules/LogManagement/routes/sys-log.ts`
- admin-lite：
  - `apps/admin-lite/src/modules/LogManagement/routes.ts`
  - `apps/admin-lite/src/modules/LogManagement/routes/login-log.ts`
  - `apps/admin-lite/src/modules/LogManagement/routes/sys-log.ts`
- zfw：
  - `apps/zfw-system-sfss/src/modules/system-sfss/routes.ts`

### 验证结果

- `packages/core`：`route-utils` 定向单测通过，`typecheck` 通过。
- `apps/admin`：路由相关测试 `4 files / 11 tests` 通过；`lint:arch`/`typecheck` 通过。
- `apps/admin-lite`：路由相关测试 `4 files / 11 tests` 通过；`lint:arch`/`typecheck` 通过。
- `apps/zfw-system-sfss`：`lint:arch` 与路由定向测试 `4 files / 12 tests` 通过。
- `apps/zfw-system-sfss typecheck`：未通过（既有 legacy 类型债务，非本次路由注入改动引入）。
- `apps/docs`：`lint` 与 `build` 通过。

---

## 2026-04-03（补充）zfw-system-sfss 接口对接完成：三链路代理可联调

### 结论

- `apps/zfw-system-sfss` 已完成本地联调链路补齐：`/cmict`、`/zfw`、`/zb` 三类接口路径均有对应代理策略。
- 登录/菜单接口继续走 `/cmict/**`，`system-sfss` 业务接口的 `/zfw/**`、指标导出类 `/zb/**` 已具备联调入口。
- 环境说明已同步到 app README 与 docs 快速手册，减少同事接手心智负担。

### 核验证据

- 代理配置：
  - `apps/zfw-system-sfss/vite.config.ts`
- 环境样例：
  - `apps/zfw-system-sfss/.env.example`
- 说明文档：
  - `apps/zfw-system-sfss/README.md`
  - `apps/docs/docs/guide/zfw-system-sfss-quick-start.md`

### 验证结果

- `pnpm -C apps/zfw-system-sfss lint:arch`：通过
- `pnpm -C apps/zfw-system-sfss test:run:file -- tests/router/registry.unit.test.ts tests/router/assemble-routes.unit.test.ts tests/router/route-policy.unit.test.ts tests/architecture/route-meta-helper-source.unit.test.ts`：通过（`4 files / 12 tests`）
- `pnpm -C apps/zfw-system-sfss build`：通过
- `pnpm -C apps/docs lint && pnpm -C apps/docs build`：通过
