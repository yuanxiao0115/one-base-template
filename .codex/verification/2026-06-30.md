# 2026-06-30 首批公共包发布链路验证

## 范围

- `@one-base-template/core`
- `@one-base-template/utils`
- `@one-base-template/tag`
- `@one-base-template/ui`

## 已通过

- `pnpm release:build`
  - 四个首批公共包均生成 `dist` JS 与声明文件。
- `pnpm release:validate`
  - 完成首批/延期包 metadata audit。
  - 完成 tracked files 凭证扫描。
  - 完成四个本地 tarball pack。
  - 完成临时 Vite 消费者 install/build。

## 未执行

- 企业制品仓库真实 publish/install smoke 未执行。
- 原因：需要发布环境提供 npm registry 凭证；仓库内禁止提交 `_auth` / token。

## 发布环境补跑命令

```bash
export NPM_CONFIG_REGISTRY=http://artifact.nc.rdcloud.4c.hq.cmcc/artifactory/api/npm/one-package/
export NPM_CONFIG_USERCONFIG=/tmp/one-base-template-npmrc
printf '%s\n' 'registry=http://artifact.nc.rdcloud.4c.hq.cmcc/artifactory/api/npm/one-package/' > "$NPM_CONFIG_USERCONFIG"
# 在发布环境把 auth 行写入 "$NPM_CONFIG_USERCONFIG"，不要写入仓库文件
pnpm release:validate
pnpm release:packages
npm install @one-base-template/core @one-base-template/utils @one-base-template/tag @one-base-template/ui --registry=http://artifact.nc.rdcloud.4c.hq.cmcc/artifactory/api/npm/one-package/
```

# 2026-06-30 admin-lite 最小 CLI 与第二批公共包验证

## 范围

- `@one-base-template/adapters@0.1.0`
- `@one-base-template/app-starter@0.1.0`
- `@one-base-template/create-admin-lite@0.1.0`
- `apps/admin-lite` 剩余 workspace 公共依赖切换为 semver

## 已通过

- `pnpm -C packages/adapters typecheck`
- `pnpm -C packages/app-starter typecheck`
- `pnpm version:packages`
- `pnpm install --lockfile-only`
- `pnpm release:build`
- `pnpm release:validate`
- `pnpm validate:admin-lite-cli`
- `pnpm -C apps/admin-lite typecheck`
- `pnpm -C apps/admin-lite build`
- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`
- `pnpm validate:admin-lite-cli`（删除模板空壳构建插件后复跑）
- `pnpm release:validate`（发布校验文案与 workspace override 注释调整后复跑）
- `pnpm -C apps/admin-lite typecheck`（补充显式安装 Element Plus 后复跑）
- `pnpm -C apps/admin-lite build`（补充显式安装 Element Plus 后复跑）
- `agent-browser --session admin-lite-cli-smoke open http://127.0.0.1:5177/`
- `agent-browser --session admin-lite-cli-smoke eval "({ url: location.href, title: document.title, text: document.body?.innerText?.slice(0, 1000) })"`

## 结论

- 第二批公共运行时包已具备 `dist` 产物、发布元数据、changesets 版本和本地 consumer 验证。
- `@one-base-template/create-admin-lite` 已可通过 tarball 生成仓库外项目。
- 生成项目不包含 `workspace:`、`catalog:`、`../../scripts`、本机仓库路径或真实 npm `_auth`，并完成 install/build。
- `apps/admin-lite` 剩余公共包依赖已从 `workspace:*` 切为 `0.1.0`，仓内验证通过。
- 删除模板空壳构建插件后，CLI 仓外生成与构建仍通过。
- 发布校验脚本完成 metadata、credential scan、pack、临时消费者构建复验。
- 生成项目浏览器冒烟可进入 `/home/index`，页面标题为 `sample-admin`，正文包含最小后台基座欢迎文案。
- 显式安装 Element Plus 后，浏览器冒烟不再出现 `Failed to resolve component: el-*`；仍有既有 Vue Router `next()` deprecation warning，非本轮阻断。
- 额外尝试 `pnpm -C apps/admin build` 失败于当前 admin workspace 解析 `packages/ui/dist/*` 的 Rolldown parse error（`Identifier h has already been declared`），不属于本轮 admin-lite CLI 通过口径。

## 未执行

- 企业制品仓库真实 publish/install smoke 未执行。
- 原因：真实 publish 需要发布环境 npm 凭证；仓库内禁止提交 `_auth` / token。

# 2026-06-30 admin-lite 企业 npm scope registry 收口

## 范围

- `apps/admin-lite/.npmrc`
- `packages/create-admin-lite/templates/admin-lite-minimal/.npmrc`
- `packages/create-admin-lite/bin/create-admin-lite.mjs`
- `scripts/validate-admin-lite-cli.mjs`
- `apps/admin-lite`、`packages/create-admin-lite` 与 `apps/docs` 相关说明文档

## 已通过

- `pnpm validate:admin-lite-cli`
- `pnpm -C apps/docs lint`
- `pnpm -C apps/docs build`

## 结论

- `apps/admin-lite` 已直接包含项目级 `.npmrc`。
- CLI 生成项目已包含 `.npmrc`，公共依赖走 `https://registry.npmmirror.com`，`@one-base-template/*` 走企业 npm。
- CLI 生成项目安全扫描覆盖 `.npmrc`，并继续阻断 `_auth`、token、workspace/catalog 协议与本机路径。
- 文档与 CLI 输出不再建议把企业 `one-package` 设置成全局 registry。
