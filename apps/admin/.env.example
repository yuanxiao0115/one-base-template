# Copy to `.env.development.local` for local development.

# Which backend adapter to use:
# - default: template mock (/api)
# - sczfw: standard-oa-web-sczfw backend (/cmict)
VITE_BACKEND=sczfw

# sczfw gateway base url (used by Vite proxy for /api and /cmict)
# Example from legacy project:
VITE_API_BASE_URL=https://gateway-sczfw-30746.p.onecode.cmict.cloud

# Optional: force mock even when VITE_API_BASE_URL is set
# VITE_USE_MOCK=true

# Optional: legacy headers (default values are already set in apps/admin/src/main.ts)
# VITE_AUTHORIZATION_TYPE=ADMIN
# VITE_APPSOURCE=frame
# VITE_APPCODE=od

# Optional: storage keys
# VITE_TOKEN_KEY=token
# VITE_ID_TOKEN_KEY=idToken

# Optional: menu root permissionCode for /cmict/admin/permission/my-tree
# VITE_SCZFW_SYSTEM_PERMISSION_CODE=admin_server

# Optional: Client-Signature overrides (defaults match legacy project)
# VITE_CLIENT_SIGNATURE_CLIENT_ID=1
# VITE_CLIENT_SIGNATURE_SECRET=fc54f9655dc04da486663f1055978ba8

# Optional: layout mode
# - side: 左侧菜单（默认）
# - top: 顶部菜单
# - top-side: 顶部展示“系统”，左侧展示当前系统菜单
# VITE_LAYOUT_MODE=side
