{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // 迁移阶段先兼容上游 utils 的类型宽松策略，后续再按模块逐步收敛为严格模式。
    "strict": false,
    "noUncheckedIndexedAccess": false,
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/__tests__/**"]
}
